# app v9 — 03 THE FOUNDER LEDGER

**Status: RESOLVED (2026-08-03). Founder: "Approve D1-D10 with your
recommended defaults." Every recommendation below is now the
decision of record. D10's mechanism stands: user-facing copy drafts
still surface for founder voice review in each phase's record.**

- **D1 — The Home lock.** The 2026-07-27 steer locked Home as the
  checklist. The brief makes body progress the emotional center.
  Proposed touch (needs your unlock): (a) the trend word joins the
  existing TodayStateBand ledger line, and (b) the lead row gets
  hero treatment in P4. Checklist structure, rail, tools, rings
  all stay. **Recommend: grant exactly (a)+(b), nothing more; body
  progress otherwise lives in Becoming.** Alternative: Home fully
  untouched.

- **D2 — Photo stance (the anti-shame call).** Options: photo-
  first with silhouette fallback / **silhouette-first with photo
  opt-in (RECOMMENDED)** / her explicit choice at first capture
  with no default. Silhouette-first fits the anti-shame brand, the
  one-colour identity law, and the JeniMethod body-image lock
  precedent (days 47/60/78/83 typography-only) — while the photo
  stays available for women who want the mirror. Cover-art use of
  her scan is always opt-in regardless.

- **D3 — Cloud backup of scans.** Recommend: local-only default,
  explicit "back up my progress record" toggle (default OFF),
  private bucket + per-user RLS mirroring food-photos, delete-
  account purge, retention cron. The 2026-07-25 incident argues
  for offering backup; sensitivity argues for defaulting it off.
  Also inside this decision: whether the gallery gets a Face
  ID/local-auth gate (recommend: optional toggle, off by default).

- **D4 — The numbers law.** Recommend hard law: no number is ever
  derived from a photo; waist exists only if SHE measures (manual
  tape entry as an optional Becoming field, displacing nothing) or
  HK provides it; composition numbers only from HK with provenance.
  If you want photo-derived anything later, it re-opens here — not
  silently.

- **D5 — HealthKit scope.** Recommend: stop requesting VO2max /
  respiratory rate / blood pressure (read-but-never-rendered
  today); keep HRV **only if** P3 ships its rendered recovery
  context; usage strings rewritten to match exactly (copy = D10).
  Cost: none visible; benefit: trust + App Review posture.

- **D6 — B2B monitoring posture (counsel-gated).** The shipped
  consent language promises "nothing is watched in real time." A
  dropout-risk flag contradicts it without a new explicit consent
  scope + reframed copy. Recommend: ship the consent-compatible
  staleness line in P6; hold the true risk flag for a counsel-
  reviewed consent revision. Scans never reach clinicians in v9
  regardless.

- **D7 — The tab's name.** Becoming absorbs the body timeline as
  its opening chapter. Rename (becoming → body? progress?) is
  identity-level — founder call, no engineering cost either way.
  Recommend: keep "becoming" until the surfaces prove themselves;
  the word already means this.

- **D8 — Onboarding tease.** v7 is freshly locked and converting.
  Recommend: no onboarding changes in v9; Body Vision introduces
  itself post-purchase (day 2-3 moment) and to existing users via
  the migration moment. A reveal-tile tease is a later, measured
  experiment through the v7 ledger.

- **D9 — The scan ritual's ask.** Weekly, her chosen day, offered
  as invitation (never debt). Recommend: no new notification; the
  scan invitation rides the existing consented daily-reminder
  surface on scan day. A dedicated scan reminder would need its
  own consent moment per the day-2 consent law.

- **D10 — The user-facing words.** Health usage strings (P0), the
  scan consent sheet, the silhouette/photo chooser, the change-
  language register ("the line and the mirror agree") — all copy
  passes through your voice review before shipping, per the voice
  law. Drafts will sit in the phase PRs.

## Sequencing note

P0 can begin on approval with zero open decisions (D5's
recommendation is its default; reversible). P1 design lock needs
D2 + D3; P2 needs D1 + D7's interim answer; P4 needs D1; P6's
risk flag needs D6. Approving the plan with "recommendations
stand" unblocks everything at once.
