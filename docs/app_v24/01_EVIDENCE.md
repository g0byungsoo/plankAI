# APP v24 — THE REGIMEN · EVIDENCE

THE LOOP's record for the medication era. Every phase lands here:
what shipped, what the films caught, what was fixed, what is
honestly deferred. `00_REGIMEN.md` is the law.

## Phase record

- **P0 docs** — law + this record initialized.
- **P1 platform** — (pending)
- **P2 notifications** — (pending)
- **P3 today composition** — (pending)
- **P4 onboarding beats** — (pending)
- **P5 regimen home** — (pending)
- **P6 patterns + becoming** — (pending)
- **P7 sync** — (pending)
- **P8 the loop** — (pending)

## Frame-caught fixes

(recorded as they happen)

## Founder gates

- Apply `supabase/migrations/20260809*_v24_medication_platform.sql`
  (dose_events + regimen_plans additive columns) — until then sync
  defers gracefully, local-first.
- Device walk: notification actions on a locked device, timezone
  travel test, the dose sheet in-hand feel.
- Review the §11 tradeoffs (no PK curve, no site photo, no
  lock-screen skip).

## Deferrals

(recorded as they land)
