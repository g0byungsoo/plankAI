# 27 — Interaction rebirth plan (v2.7) — as executed

Short by design; the audit (26) carries the verdicts.

1. Clipping contract across the component system → DONE, SE-verified.
2. State band awakening ON VISIBILITY (not mount) → DONE,
   frame-diff-proven.
3. Strike-as-moment (delay past cover, pen speed, tick cascade) →
   DONE.
4. Lesson close rep chip (interactive CBT commitment) → DONE,
   walker-proven; content reps live on D02/D03/D19/D25, template in
   doc 22 for the sweep.
5. Cross-account macros fix (found by motion QA) → DONE.
6. Verified-good-already: breath bloom core, trend scrub, reader,
   snap carousel — left alone on purpose (restraint).

## Verification artifacts
- /tmp/jenifit_motion: today_cold_open.mp4, today_full.mp4,
  band_awaken.mp4 (+ frame dumps, pixel-diff logs).
- Ledger adds: 95/96 (rep chip both states), 97/98 (SE width).
- Motion QA method now documented: record → ffmpeg frame dump →
  PIL pixel-diff → read the decay curve (ease-out signature) →
  eyeball the mid-animation frame.
