# AGENT EXTRACT — food pipeline code audit (a3a39788)

DOORS (all → CapturedFood; EntryMethod stamped at ONE chokepoint FoodCaptureDispatcher:102-106): photo 4 taps cold (+2 one-time gates); barcode = live detect no shutter, OFF v2, confidence 1.0, per-serving leads, servings-per-container derived (2-24 guard), unknown code → AUTO-MORPH TO LABEL MODE + "the label works every time" (PhotoCaptureView:1375-86); label = same EF labelHint, NutritionSource.labelDeclared at chokepoint; words = 3 taps + type, auto-submit, spoken=true; again = 3 taps, EntryMethod.again, food_log_saved inside persister; jeni chat log_food_text NEVER writes — opens describe prefilled (JeniActTools:229-246).
Failure: deadline+3 transient retries; empty → "one more look?" retry card; rate-limit terminal sheet; offline copy. WRONG-but-known barcode: NO defense (confidence 1.0; recourse = editor).
Dormant: quickAdd pantry .notImplemented; imOutTonight no callers.

READING: one page all doors; PlateEditSession; title/item edit; portion steppers quarter-grid [0.25..4]; PlateShare ladder (solo all/¾/half/bites; shareable from model's servings_in_dish; packaged COUNTS UP to package ceiling); macro↔kcal Atwater coherence; physics clamp ≤9kcal/g; provenance = EntryMethod.provenanceLine (photo "read from your photo · ranges, not exact"; label/barcode "copied from the label"; words "logged from your words"); kcal rounded to 5; ±band when half-range ≥20; low-confidence footnote <0.65; micros gated on WHOLE plate grounded.

**DEFECT (new, 5th instance of defaulted-init field-drop family): PlateEditSession.withNutrition/scalingNutrition/withIdentity (SnapResultMath:293-391) re-init CapturedItem WITHOUT micros: → any stepper/fraction/editor action silently drops that item's micros → partial-sum micros panel (exact defect namedMicros guards) or vanishing panel on grounded plates. PlatePriors.scale carries micros explicitly + documents why (PlatePriors:221-234). Display-only (micros never persisted).**
**DEFECT: FoodLogSyncRow decodes NULL source as "photo" (SyncService:2320) — invented attribution on hydrate; write-side law says absent→unknown (CapturedFood:430-438). Pre-D3.B rows claim "read from your photo".**

CORRECTIONS: two mechanisms. Fix-with-words → SnapRefine (scope-guarded merge: only mentioned items move, hallucinated additions die) → appliedCorrections persisted in payload jsonb, readable (wasCorrected), "your numbers" ledger, jeni your_corrections. **ONLY writer = SnapRefine.fixWords — stepper/editor edits fire analytics but write NO correction → never become priors (flywheel gap).** PlatePriors: corrected+kcal>0 rows, EXACT normalized title key, latest wins, PHOTO DOOR ONLY (words exclusion reasoning quoted: "a prior must never overrule a portion the user stated herself"), ±15% band, [⅓,3×] clamp, visible+revertible. Priors index is DOOR-BLIND (a package correction can rescale a later photo of same-titled dish — cross-provenance, clamped, revertible).

REPEAT: RecentMealsSheet = 6 recents, RECENCY ONLY, title-deduped; relog copies every nutrient + corrections ("THE RECORD MUST COMPOUND, NOT DECAY"), fresh id/Date, no photo, source again. NO favorites / saved meals / recipes / copy-yesterday (grep-confirmed zero). Relog doors: chooser, plate page, book long-press.
≤3-tap repeat verdict: YES exactly 3 (scan tab → again row → meal row) WITH corrections intact. Breaks: 6-slot eviction (fallback book = 5+ taps); manual-edit corrections invisible to flywheel; re-photograph ≠ relog (needs words-fix + exact title); one-time consent detour exits to CAMERA even for words entry (CaptureFlowView:87-130); two-device gated hydrate.

EDIT/DELETE/HISTORY: setLoggedDay (working tree): moves day keeps clock, ≤now, id preserved (photo travels, cloud UPDATE), 14-day rule in UI (PlateDetailSheet dayOptions 0..<14); delete → tombstone BEFORE network (AppSync:104-128), cloud row+photo deleted, sweep after hydrate re-deletes resurrections; THE BOOK: day spreads, dayLedger above photos, month seams, tap→PlateDetailSheet, long-press relog. Retention: lifetime (stale doc comment claims 14d TTL at :874 — dead since 06-11).

SYNC/OFFLINE: JSONL append-only store; upsert fire-and-forget NO food-row retry queue (launch push-diff = the net); hydrate ONLY when a family is empty AND once/day (AppSync:2545-80) → engaged device may never hydrate; mergeRemote INSERT-ONLY → cross-device edits never propagate; photos: bucket STILL doesn't exist (deliberate, deletion-ordering), 500-cap queue, 3-failure stand-down, once/day sweep; two-device delete = standoff flap documented.

MODEL: Entry{id,userId,loggedAt,kcal,protein,carbs,fat,fiber,sugar,sodiumMg,satFatG,title,items,source,itemsDetail(name,portionG,kcal,p,c,f,±sodium,satFat — NO sugar/micros/per-item source),corrections}. NOT stored: dayKey (derived at read!), timezone, micros, per-item NutritionSource, confidence, ranges, priorApplied.
TIME: loggedAt=Date() at persist/add-it; meal label by hour (no midnight label); midnight defect = manual repair via setLoggedDay; **no per-entry timezone → cross-timezone travel silently re-attributes days on every reader.**

PROVENANCE TAXONOMY: AI estimate / label fact / DB fact / user-corrected all first-class PRE-PERSIST; conflated AT persist: per-item NutritionSource dies (only door survives); manual edits invisible; NULL→"photo" on read.
