# app v3 — honest remaining gaps

Date: 2026-07-05. What was designed but not shipped this pass, what
was shipped thin, and what was found but deliberately left. Nothing
here is forgotten; everything has a reason.

## Designed, not built (docs carry the spec)

1. **Notifications orchestrator — SUBSTANTIALLY BUILT 2026-07-06.**
   Shipped: the keeping-chapter ZONE-CROSSING push (weigh chokepoint
   consumes BandModel crossings → next-morning care push, steady
   recovery stays in-app), the WEIGH-PATTERN watcher (+8-day re-armed
   one-shot, "the line misses you"), and the LAPSE-SUPPORT ping
   (weeks 0–6 only, 20:30, cancelled the moment a plate lands,
   mutually exclusive with the evening review, deep-links to the
   breath brake). All three follow the 4-site id protocol (schedulers
   / cancelAll / AppSync+BreakState sweeps / delegate map — the map
   leg is unit-pinned) and sleep on a break. Week-1 milestone bloom
   already ships via the presence ledger (day-3 milestone).
   STILL DESIGN: the anchor-ask-at-first-kept-moment (founder copy +
   post-purchase flow placement) and reading-bodied anchor rungs
   (the ladder speaks archetype lines — deterministic-safe; the full
   reading needs live state pushes can't precompute).
2. **Weekly receipt → consent-based check-in.** The MacroFactor
   re-prescription mechanic ("this week taught your plan: protein
   eases 5g. keep it?") is specified in 01_VERDICTS; the Sunday
   receipt artifact is unchanged this pass.
3. **"Hold here, or keep going?"** The keeping chapter derives from
   existing keys; the explicit entry question (migration + goal-
   reach moments) needs founder copy + flow placement.
4. **One-word check-in chips** (food-noise/appetite EMA seed) —
   specified; only the on-medication evening sit-check shipped.
5. **Widget + HealthKit body-mass import + cross-device chat** —
   stretch list, untouched.

## Shipped thin (works, wants deepening)

6. **Rep content**: 10 authored scenarios + 6 pillar fallbacks cover
   every lesson day, but the 84-slot bespoke pass is founder-present
   work (RepEngine's table is the template).
7. **Per-plate "how did it sit?"**: shipped at DAY level (evening
   receipt). The per-plate chip lives in Packages/PlankFood surfaces
   and awaits a package pass.
8. **Band on the canvas**: zones clamp to the chart's y-domain; a
   keeping user whose line sits far from settle can have the band
   render partially offscreen. Extending yDomain to include the band
   is a small follow-up.
9. **Chat letterhead**: the reading seeds as jeni's first message of
   the day (engine-identical to Today) + her file card; a pinned
   letterhead treatment was considered and skipped (the seeded
   message reads naturally in the letter register).

## Found, deliberately left (pre-existing)

10. **Dead beat cases** (.plank/.water/.measurements) + the dual
    title metadata on ProgramDayPrescription — removal touches
    MarkAsDoneSheet's exhaustive switches; scheduled hygiene, not
    risk.
11. **"−2.2 lb this week" badge vs "eased down about 500g" story**
    on Becoming measure different windows (raw 7d vs EMA 7d) — a
    copy-alignment question for the founder (both are honest).
12. **Workout preset descriptions** (sentence-case strings) + the
    reader-length audit — pre-existing follow-ups, unchanged.
13. **v5 walker red-heart screenshots**: code carries U+FE0E; the
    old PNGs predate the fix. Re-verify on the next onboarding
    walker run (not run this session — onboarding untouched).
14. **Steps "0" trailing numeral** on the rhythm row reads cold at
    0 steps (sim-only in practice; devices show live counts).

## Verification debt

15. **Motion archive**: reading-settle + rep-door recordings captured
    this session (see VERIFICATION_REPORT); snap/food-log/weight
    end-to-end continuous recordings remain the archive's missing
    pieces (walker-green, stills verified — same status as v2.9).
16. **Dynamic Type XL pass** on the new surfaces (reading block,
    one-thing card, rep doors) — relativeTo metrics are wired
    everywhere; an explicit XL walkthrough is still owed.
17. **Onboarding→paywall→purchase E2E** was not re-walked this
    session (no onboarding/paywall code touched; v5 walker last
    green 2026-07-03). The states leg re-verified wall + migration
    on today's build.
