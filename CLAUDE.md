## Project status (2026-09-01) — THE FINISHING PASS

**THE FINISHING PASS (feat/app-v2). BUILT 2026-09-01, after 61.**
`docs/app_v25/62_THE_FINISHING_PASS.md` is the record. The founder's
brief: end the quality variance — one interaction system, drift debt
paid, the app held before and after every change. ① **ONE DIRECTOR
EVERYWHERE** — becoming was the second, unreformed director: the
weekly read scheduled from `refresh()` (every plate log/scan/scope
tap), stamped its once-per-week flag AT SCHEDULE TIME, was blind to
its five sibling covers, and a plain tab switch into becoming
scheduled NOTHING. Now `BecomingAutoPresent` (pure, 6/6): arrival-only
(tab arrival · appear · foreground), ONE settle beat, stamp on actual
present, materialize like its siblings. **PresentationGate is an owner
SET** (shell·home·becoming): every cover owner raises it, every
director asks "anyone up besides me?" — reconcile can no longer burn
its flag into an open BOOK; the scan chooser holds the slot too.
② **The presentation grammar crosses the package boundary** (p61 §8
closed): `foodSheet` mirrors the app fold exactly (tokened detents ·
28pt corner · paper ground · grabber); PresentationGrammarTests walks
PlankFood too (went honestly RED on ShareActivityView first).
③ **One commit beat** (`JeniMotion.commitDwell` 0.45 + `receiptDwell`
1.5 — four hand-picked speeds unified, the weight ritual's hold now a
NAMED beat); the p58 record hand reaches MoveSheet + SideEffectSheet;
the upgrade cascade speaks kit tokens (CTA ~0.4s, was 1.15s).
④ **The upgrade cover scrolls** (p54 min-height law on the one
commerce surface without it; AX used to clip BOTH edges, X and CTA
unreachable) — and the iteration caught two of my own regressions on
film (SE broke "$29.99" mid-number; the AX5 price row re-widened the
page → price keeps fixedSize+priority, stops growing at AX2).
⑤ **Drift debt**: ONE masthead scrim law (solid through the status
bar — becoming's serif read through the clock); the close's fold-line
fades ("on tile." frame-catch); `Radius.tile=16` named (28 raw sites)
+ every literal that already had a token converges (38 files);
`jeniContactShadow()` callable (chat double-layer, .black one-off,
gauge glow all die); ~976 dead lines out (LastNightSleepCard +
harnesses, JeniMoment — §6.5 amended to keep the LAW past the
component's death, LabReadout pair); keyboard dismisses interactively
on nine text surfaces. ⑥ **Words/trust**: "not enough to read yet" →
"still filling in" (the header contradicted the values beneath it);
a repeated identical edit note prints once (RED with the film's exact
doubled "had half of it", GREEN with half-of-half=265 pinned).
⑦ **Refused on evidence**: RegimenSheet's 55-spacing rewrite (filmed
clean — invisible churn); the reveal's 49 spacings (conversion
surface, no funnel data — deferred); clinical-family underlines (a
consistent family grammar, 16 sites); VisitPacket "drift" (it's the
PRINT renderer); Stickers deletion (the audit overreached —
StickerStyle is live). **PROOF: app 1592 · 2 skipped · 0 failed
(1584 + 6 becoming-director + 2 gate, exact) · PlankFood 289/289
(+1 RED→GREEN) · PlankSync 29/29 · Release BUILD SUCCEEDED.** Filmed
before/after: becoming scrim · weekly-read arrival · repair loop with
the new sheet chrome · evening close rest+scrolled · stated plate ·
upgrade cover 16/SE/SE-AX5/16-AX5. Market refresh (Sept 2026): the
category's trust collapse (MFP 3.24→1.54, class action) → the law
"never relocate the record, never add taps to the core loop"; Liquid
Glass stalled (~15-18% adoption) — the standing refusal is
market-validated. **Founder-thought flags (not built): her-own-pattern
cycle rhythm · one-tap food-noise door · maintenance era.** **No
migration, no schema, no production mutation, no deploy. NOT
ARCHIVED, NOT UPLOADED, NOT SUBMITTED.**

## Project status (2026-09-01) — THE PRODUCT LEVEL

**THE PRODUCT LEVEL (feat/app-v2). BUILT 2026-09-01, after 60.**
`docs/app_v25/61_THE_PRODUCT_LEVEL.md` is the record. The founder's
brief: one meaningful level forward — the actual product, not a
document. Three parallel audits (entry choreography · design drift ·
the food pipeline), fresh category research, every change walked on
film. ① **THE RECORD IS THE READING** — the gravest find: persist
stored the model band's MIDPOINT (a required schema field → every
photographed plate) while the reading summed the items; she agreed to
one number and the product kept another, in the dial, Apple Health,
the envelope and the packet. `CapturedFood.recordedKcal` is the ONE
rule now (RED 7/10); the physics clamp reaches the record and skips
unknown mass. ② **THE FILED PLATE IS CORRECTABLE** — repairFood +
updateEntry + `PlateRepairSheet` (the scan-time editor reopened on the
filed entry; same id/day/door, corrections intact, persist's own
arithmetic; the plate page re-reads itself). "off? fix this plate"
leads; remove stays. ③ **StatedPlate** — "protein bar, 190 cal, 20g
protein" files instantly, `userStated`, nothing invented, "your
numbers, as you gave them". ④ Trust sweep: failed saves say so (the
catch used to dismiss 1.15s after the success haptic); today-moves
clamp to now (RED 2/3); a new-name-only spoken rename swaps instead of
doubling (RED 1/3, addition-shapes never delete); words door gets the
45s deadline + a true-cancel X + millisecond offline fail
(delivered-verdict NWPathMonitor); label door refuses false provenance
(is_nutrition_label read at last; usdaOverride demotes "copied from
the label"); BOOK + again rail obey numeric suppression; absence
prints as absence ("not counted", no "0 g" costumes, unstated macros
print "—"); nutrition telemetry rides the FoodAnalytics boundary
(names/barcodes gone) and the hygiene registry gains the events with
the law SHARPENED (Doubles per-key pipeline-numeric only; arrays as
word-lists — the mechanism caught my own unregistered key as a DEBUG
crash mid-walk, as designed). OFF present-zeros stay absent (the pin
is right; reversed my own change). ⑤ **ONE INTERACTION DIRECTOR** —
the evening close joins HomeAutoPresent (reconcile › evening close ›
letter › upgrade; the morning read stands down all evening; arrivals
only — appear/foreground/midnight); ONE settle beat (0.6s); ONE cover
grammar (instant materialize, moment owns motion); PresentationGate
exposes the shell's surfaces to the arbiter; the director holds while
ATT is owed and resumes on `.attPromptSettled`; the upgrade's fetch is
bounded; Home consumes a cold-launch pendingRoute on appear; the
cascade steps once per block (indices 0…6, async gate flips animate);
`.appSyncDidHydrate` ends the 35s stale-numbers window; the
atmosphere runs 20fps and pauses under covers. ⑥ **THE OLD PARTS
LEAVE** — ~2,900 lines of grep-proven dead code deleted
(JKSignalVisuals 1361L, StepsBentoTile, ChapterCompleteView, 17/23 of
Components.swift, +9 more); both scrapbook chromes now render the
§6.1 material (the pre-paywall reveal — every surviving call site —
wears the app's own surface, filmed before/after); the food rail's
geometry comes home (gutter 20→16, radius 24→22); SF Rounded + New
York leave; sticker shadows die; the settings pearl loses its baby
blue. **PROOF: app 1584 · 2 skipped · 0 failed (1577 + 4 arbiter + 3
hygiene, exact) · PlankFood 288/288 (253 + 35, every RED honest) ·
PlankSync 29/29 · Release BUILD SUCCEEDED.** Filmed: home entry ·
letter · BOOK · plate page · the repair loop (half → 270 → every
consumer moves) · the stated plate → filed answer · scan
slow/timeout/empty · the reveal · SE standard + AX sizes. **No
migration, no schema, no production mutation, no deploy. NOT
ARCHIVED, NOT UPLOADED, NOT SUBMITTED.** Named not done: the reveal's
deeper spacing pass · the package presentation-sweep boundary ·
two-device food pull · book swipe/undo · founder thought: maintenance
era, cycle-aware energy, clinician export.

## Project status (2026-08-28) — APP REVIEW: ATT + EULA

**APP REVIEW: THE ATT PROMPT AND THE EULA LINE (feat/app-v2). BUILT
2026-08-28, after 59.** `docs/app_v25/60_APP_REVIEW_ATT_AND_EULA.md`
is the record. Apple rejected 1.1.7 (35) (submission b7b6a6d4; iPhone
17 Pro Max iOS 26.6.1 · iPad Air 11" iPadOS 26.6): **2.1** — the ATT
prompt could not be located; **3.1.2(c)** — no functional Terms of Use
(EULA) link in metadata. **ROOT CAUSE, from code:** the ONLY
`requestTrackingAuthorization` in the binary sat at 30% of the
plan-building loader — reachable only by finishing the whole consult,
only on the `.loss` safety path, only first-run (returning/expired/
restored users on `.wall`/`.main` were NEVER asked) — while
`PlankAIApp.init()` initialized the TikTok Business SDK 1.6.1
(Install/Launch/Retention/Purchase auto-events) unconditionally BEFORE
ATT on every launch; TikTok 1.6.1 never auto-presents ATT, and the sim
masked all of it. **FIX: `ATTService`** (PlankApp/Analytics) — one
source of truth: pure `ATTFlow` core (+`ATTAuthorizing` seam), prompt
at the FIRST SETTLED SURFACE of any launch (RootView `.task(id:)`,
gate = scene active ∧ phase ≠ .booting — deliberately wider than
`isStable`, which excludes .wall), fixed 0.6s settle beat, re-arm only
when iOS declines to PRESENT, an answered prompt never re-asked;
**TikTok init gated on ATT resolution** through
`ATTService.configure` (resolved-at-launch = old timing; SKAN
untouched); loader call kept as secondary belt; DEBUG-only suppression
under XCTest/`--uitest*`/`--debug*`. **PROOF: app 1577 · 2 skipped ·
0 failed (p59's 1565 + exactly the 12 new `ATTServiceTests`) · Release
BUILD SUCCEEDED · ATT string + NSPrivacyTracking verified in the built
Release product.** Prior fixes verified standing: billed-TODAY leads
every paywall row (3.1.2(c) p56), downsell machinery deleted (5.6).
**FOUNDER (ASC, exact text in the record §6-§7):** App Description
gains `Terms of Use (EULA):
https://www.apple.com/legal/internet-services/itunes/dev/stdeula/`
(verified live) + keep jenifit.app/terms + /privacy; attach a
physical-device recording (fresh install, Tracking toggle ON → prompt
over the first consult screen ~1s in; iPad too); verify the LIVE
description/label (repo `docs/app_store_metadata.md` is still the
v1.0.0 plank draft claiming "no advertising trackers" — false with
TikTok; never let that text reach the listing). **No version bump, no
paywall logic, no schema, no production mutation. NOT ARCHIVED, NOT
UPLOADED, NOT SUBMITTED.**

## Project status (2026-08-25) — THE HOME DESIGN PASS

**THE HOME DESIGN PASS (feat/app-v2). BUILT 2026-08-25, after 58.**
`docs/app_v25/59_THE_HOME_DESIGN_PASS.md` is the record. A focused
product-design pass on Home; correctness preserved, visuals re-decided
by LOOKING (`--debug-home-redesign` harness, the p58 §8 method widened
to the whole page), then steered FOUR times mid-pass by the founder
and the steers taken. ① **THE DIAL** — the nutrition band is a
156pt/15pt centered remainder dial (protein iff a floor exists — §9's
law — else calories): what is LEFT lives INSIDE the ring
(`58 · g to the floor`, counting down as plates land; met = the
strip's drawn check + `floor met`), THE MINIS beneath (fourth steer,
kcal deprioritized): `sugar` badge · `fiber · dv` gauge · `kcal left`
gauge — 52pt echoes of the parent, rendered only for what the day
measured; carousel faces the record earns — THE PLATES (her photos as
a 2×2 gallery; typed plates seat the dish's serif initial) and THE
NUMBERS (carbs · fat · sodium as a set table; fiber/sugar live in the
minis — each fact in ONE place). Only collected targets speak "left"
or draw a gauge (no invented denominators; sugar's total-vs-added
refusal stands — its mini is a hairline BADGE; fiber gauges the FDA
dv with the dv NAMED). AX sizes keep a words-and-thread receipt
(`JeniFloorThread`, floor tick at 82%); suppression keeps the
words-only face; every sentence law stands (lead rule, remainder
word, count-up silence, `· holding`, absence prints nothing) — the
pinned suites green throughout. ② **THE MASTHEAD** — the pink day
capsule became a SET dateline (`DAY 12` Fraunces caps · hairline
rule · the week's word in serif italic); same doors (tap = letter,
hold = settings); caps at XXXL (AX5 frame-caught truncation, fixed).
③ **THE DOSE OBJECT** — the standing is a hairline-bordered clinical
row (ink seat, SF glyph), not a floating text pair. ④ **THE DAY
OBJECTS** — JeniTaskRow grew to 52pt seats / 16.5pt titles / 26pt ink
stamp; done compresses to a 28pt receipt carrying the plate's own
photo; the offered seat is a SOLID hairline (dash read as wireframe).
⑤ **THE TOOLS INDEX** — the tile grid became five hairline rows
(word · state · bare doodle); instruments moved to where their facts
now live; `JeniToolTile` deleted. New door: `--uitest-band-face N`.
**PROOF: app 1565 · 2 skipped · 0 failed (the p58 baseline exactly) ·
PlankFood 253/253 · PlankSync 29/29 · Release BUILD SUCCEEDED.**
Filmed: 16 · SE · SE-AX5 · 17PM · fresh day · floor met · count-up ·
suppression · all three faces · land-plate morph · strip walk.
**No migration, no schema, no production mutation. P0: 0 · P1: 0 new.
NOT ARCHIVED, NOT UPLOADED, NOT SUBMITTED.** Named not fixed: the
bottom tab-bar fade is weaker than the top scrim (pre-existing
chrome); the past-day recap card wears the older grammar; the QA
seeder's violet plate hue (the p25 stand-in law, again).

## Project status (2026-08-25) — THE RECORD OUTRANKS THE QUESTIONNAIRE

**THE RECORD OUTRANKS THE QUESTIONNAIRE (feat/app-v2). BUILT
2026-08-25, after 57.** `docs/app_v25/58_THE_RECORD_OUTRANKS_THE_
QUESTIONNAIRE.md` is the record. The founder's brief re-opened five
p57 gates; walking found the unifier: ① **MEDICATION TRUTH** — a
GLP-1 persona's Home said "your shot is today" AND "187 over" in ONE
frame: every cohort consumer read `onboarding_glp1_status` while the
regimen record aged beneath it (regimen editor never wrote the key;
clinic door writes none). BELT: `RegimenService.reconcileCohortStatus`
at the regimen chokepoints (active plan → "current"; ending the last
→ "past"; an unconfirmed answer is HER word, never rewritten) +
UserRecord mirror. BRACES: record-aware reads (`Chapter.derive` +
`hasActiveMedicationRegimen`, `CohortStore.isOnMedication` threaded to
the snapshot's ONE chapter, the reading's count-up, both protein-floor
resolvers). RED 5/8 no stubs; refilmed count-up + 120 g floor.
② **[CORR p37+p57] came_for NEEDED NO COLUMN** — the outcome answer
has ridden `users.onboarding_motivation` since v5 and hydrates back;
the whole fix was ONE merge line in `restoreCohortDefaults` (RED 1/3).
③ **ONE ERA ARITHMETIC** (`RegimenEras`, pure): three consumers
counted a SCHEDULE-only change as a dose change ("picked up after the
dose changed" could lie; envelope era_count inflated; ledger rows
split) — RED 1/6 at the real envelope; **dose-era seams shipped on
the 200pt weight detail** (v24's prepared design: dotted hairline +
the new dose's own word, under the ink, window-edge-suppressed,
VoiceOver parity; face/hero clean; delta-per-era stays on the ledger).
④ **THE WIDGET BUILT** (p57 gate ① opened): greenfield JeniTodayWidget
small+medium — App Group `group.com.bk.plankAI`, ONE precomposed
contract (`JeniWidgetSnapshot`, dual-membered; the widget holds no
engine), words PINNED EQUAL to the band's grammar (count-up never
"over" on the Home Screen), civil-day truthful (fresh-day at
midnight), suppression = no numerals, dose line keeps DoseStanding's
discretion + `.privacySensitive`, sweep retires the snapshot; brand
fonts embedded; `--debug-widget-gallery` films all 7 faces (zero-dot
caught on film). ⑤ **JeniHaptic gains `record()`** — dose/weight/plate
commits one hand; design law §8 amended. ⑥ **AGE1 written NOT applied**
(docs/58_packages/; no CHECK by design; client diff documented; client
MUST NOT ship first). ⑦ **The band re-decided BY LOOKING**
(`--debug-band-contenders`: split donut unreadable-as-facts;
remainder-hero = shame numeral + count-up absence; A stands). Liquid
Glass refused again on Aug-2026 evidence. **[CORR p57 §37: baseline
declared 1540 test funcs TOTAL — the skipped pair was double-counted.]
PROOF: app 1565 total · 2 skipped · 0 failed (+25 reconciled exactly:
8 truth + 3 outcome + 6 eras + 8 widget; first run's 2 failures were
MY shared-container seeds — the p36 lesson — fixed in my files) ·
PlankFood 253/253 · PlankSync 29/29 · DayKey 3/3 ar_SA · Release
BUILD SUCCEEDED. Protected paths EMPTY except the two entitlements +
widget Info.plist (App Group + fonts, named). NO production
mutations.** FOUNDER: apply AGE1 then the documented client wiring ·
verify App Group provisioning at first archive · widget placement =
device step · standing gates unchanged. **P0: 0 · P1: 0 new. NOT
ARCHIVED, NOT UPLOADED, NOT SUBMITTED.**

## Project status (2026-08-25) — THE APP PEOPLE KEEP

**THE APP PEOPLE KEEP (feat/app-v2). BUILT 2026-08-25, after 56.**
`docs/app_v25/57_THE_APP_PEOPLE_KEEP.md` is the record. The first
product-depth pass after the correctness line: research-first (93 GLP-1
+ 79 food-tracker frames all opened; 20+ fresh web sources; six
parallel audits), customer walks before code (a new scriptable
walker-arm, `DriveUITests`). ① **THE PRESENTATION GRAMMAR IS A
CHOKEPOINT**: `jeniSheet`/`jeniCover` are the only legal presenters,
held by a DEBUG-stripped source-sweep test (RED 41 bare sheets + 13
covers + 2 grabber-hiding files); the grabber is ALWAYS visible,
covers name their exit, `brief`/`tall` carry a `.large` escape.
Closed: the 0.42 no-scroll record sheet (D1), the no-dismiss modal
with unreachable exits (D2 — exits pinned, dismissal law kept), the
two exit-less `.large` sheets (D4), fake grabbers (D5), the packet as
the one record-sheet among covers AND with no close control at all
(D6 — a page with an X now), the scroll law on 6 more sheets (D7),
the settings hub's pseudo-push (D8 — real NavigationStack, edge-swipe
pop, drafts block swipe-dismiss only while unsent words exist), and
the auto-present race that could mark a morning letter delivered and
never show it (D3 — one arbiter, reconcile › letter › upgrade,
commerce never outranks the read, flags stamp on actual present).
② **BODY SNAP LEFT THE SHIPPING EXPERIENCE** (founder decision; 72
weight-loggers : 8 scan-keepers): six entrances removed, custody kept
— records, deletion, backup, and data-gated READ access for
scan-holders; @Model untouched; the plan-invitation pin inverted.
③ **THE CONSULT KEEPS ITS PROMISES** (RED first): postmenopause +
postpartum now actually get the gentler pace + Hard lock they were
promised (`gentlerPaceStage`, peri-specific consumers deliberately
narrow); "week three is where it usually breaks. we plan for that"
finally has a keeper — the weekly read speaks it once, week 3, only
to someone who said she'd tried before. ④ **THE ANSWER SURVIVES**:
the three-questions offer no longer covers the first plate's answer
sentence mid-read (filmed byte-stable 3s); and **"over" obeys one law
on both surfaces** — the reading's day line now keeps the same
count-up silence Home has kept since p53 (pure seam
`FoodModule.dayLine`, cohort threaded). ⑤ **HOME SPEAKS ONE GRAMMAR**:
ring (the only earned shape) + one kcal sentence + one NBSP-bound
rest line; the split-bar, dot legend, chemistry grid and dv footnote
left for the study surfaces; the band costs half its height; filmed
17PM/SE/SE-AX5. Weight prints one grammar everywhere
(`WeightLedger.number`); a one-item plate shows one stepper.
**Liquid Glass refused as a theme, deliberately** (evidence: users
reward readability; the identity is the moat) — native BEHAVIOR taken
instead. **PROOF: app 1540 · 0 failed · 2 skipped (+20 vs 1520
reconciled EXACTLY: +5 grammar +4 arbiter −4 stale scan +2 pins +4
consult +4 week-three +5 rest-line) · PlankFood 253/253 (+4) ·
PlankSync 29/29 · DayKey 3/3 ar_SA · Release BUILD SUCCEEDED · p56
reviewer walks re-run green on the retrofit (4/4 + 1/1 + 1/1).**
4 RED artifacts in `57_evidence/`. **VERSION COMMITTED: 1.1.7 (35)
all four sites** (the founder's post-56 §11 decision, widget aligned).
**No SQL, no deploys, no migrations, no sim erases; standing QA
identities reused; production mutations NONE.** NAMED NOT BUILT
(founder): the widget (next-dose + protein-left — both GLP-1
competitors' core retention surface) · dose-era badges on the weight
trend · the `came_for` outcome column · the exact-age column · the
activeEnergy weekly-read line. **P0: 0 · P1: 0 new. NOT ARCHIVED,
NOT UPLOADED, NOT SUBMITTED. ANOTHER AUTONOMOUS PASS: NOT JUSTIFIED —
what remains is founder decisions, hardware, and real users.**

## Project status (2026-08-19) — THE JENI METHOD

**THE JENI METHOD (feat/app-v2). BUILT 2026-08-19, after 53.**
`docs/app_v25/54_THE_JENI_METHOD.md` is the record (the brief's 34
questions, answered in order). 53 made the record answer back; **54
made the Method the interpretation layer over it: twenty notes, each
triggered by HER data, each carrying a machine-checked evidence tier,
each with a follow-up the weekly read reports back — and the 84-lesson
course is finally DELETED, not merely unreachable.** ① **METHOD CORE,
11 defects RED→GREEN (13 fail/76 first run):** M1 — `lateInDoseWeek`
fired MID-CYCLE for every-N users (builder mapped non-daily→"weekly",
engine hardcoded 5...7; a q10d user heard "the hungry end" on day 6 of
10 and silence on the true waning days) → the gate is now
**`CyclePosition.band == .waning`**, the engine's own arithmetic, copy
speaks `cycle_word`; M8 — `method.ledger.v1` in NO sweep (next account
inherited the told-list; trigger names are health-state descriptors) →
swept, the §38 lesson again; M6 clinic note now pins the day; M7 tile
read `.last` of a newest-first list (oldest note forever); M9 pattern
window excluded today; **M10 — version fields were inert across two
rewrite passes → FNV-1a content-fingerprint tripwire: rewriting copy
without bumping the version fails a test** (all rewrites v2, catalog
v2); M2 — four `because` lines made external claims tiered
`.arithmetic` w/ nil evidence → re-tiered + **claim-marker sweep**
(external science requires tier + evidence line). **NEW NOTES,
evidence-gated:** `menses_scale_bump_v1` (.strong, White 2011 — "up
{bump} with your period starting… water, then let it go"),
`medication_ended_v1` (.strong, STEP-1-ext-informed but
support-shaped; chatSeed FORBIDS the restart/medication lane),
`salty_dinner_pattern_v1` — **N-of-1 via the {nth} token: the builder
counts HER sodium→bump co-occurrences; <3 → token unfillable → drops
to the population note; `ordinalWord` refuses past "sixth"**. Note 3
plateau rewrite: adaptation myth OUT (Hall/Martins: drift, ~40-90
kcal/d), action = tighten the record; note 5 hedged (no published
day-pattern; PK-label evidence, RP tier); note 15 carries Wharton 2022
(24.2% vs 11%). Frame-caught: **bump notes spoke kg to lb users** →
`weightUnitIsLb` + unit pin. ② **ONE PLATEAU, ONE FOLD FINISHED:**
`WeightWeekReadEngine.flatWeeks(trend:)` + `plateauFlatBandKg` is THE
plateau arithmetic (Method, `isPlateauWeek`, weekly read);
`BodyState.isStalled` removed; `plateauHold` copy fixed; InsightEngine
REWRITTEN (dead card corpus deleted, `trendStory` band-gated,
provisional = "an early read."); chat trend card + envelope delta
gated on established (+ `no_trend_note`, `treatment_months_basis`,
in-band `season_note`). ③ **WEEKLY READ = WHAT ACTUALLY MATTERED:**
follow-through ("2 of 3 notes jeni left this week were followed by the
move they named" — the JITAI loop visible to HER), weekend attribution
("weekends ran about 400 kcal above your weekdays, and the weekdays
held" — gated ≥2wknd/≥3wkdy/≥150/round-50, never under suppression),
protein delta upward-only, strength held, waning teaching
(interval-aware "dose rhythm"), tenure≥10mo plateau teaching ("the
trials' own curves flatten"), close = **"nothing needs a reset."**
④ **NOTIFICATIONS: the arbiter was three-quarters inert** (4 admit
sites, 13+ bypasses incl BOTH daily repeaters; master-off left 5
families alive and `reschedule()` re-armed them; the ledger
self-saturated so the budget vetoed the kindest push; worst real day
8 deliveries). RED 3 → GREEN: **`NotificationGate` chokepoint +
`#filePath` source-sweep test** (a new `center.add(` outside
gate/med/DEBUG fails); consented cadence (medication · morning read ·
weekly read) exempt-NOT-stamped vs budgeted interruptions ≤5/wk;
**DELETED under presumption: affirmations family, `daily_reminder`
(cancel-only stub sweeps installs), milestone pushes, first-log nudge,
RecapNotificationService**; `evening_plate_review` → one-shot re-armed,
cancelled when a plate lands; `NotificationCensus.retiredIds` sweeps
old installs; master toggle finally means off. ⑤ **CORPUS DELETED,
~48.4 MB, unreachability proven FIRST:** Reader/ (18 files, 3 .metal),
RepEngine, CBTCurriculum*, manifest_v1.json, 42 jm_hero_*, Ritual
family, JeniMethodRitualSafetyTests (13 tests); kept
JeniMethodState.markEnrolled (live), FeatureFlag, RitualMusicPlayer
(breathwork uses it), LessonCadence; the lesson BEAT verified alive
(mounts MethodNoteView, silence → methodTold). pbxproj −132.
⑥ **ADVERSARIAL A–M, 9 pins:** same +1.6 lb explained on a flat trend,
NO reassurance on a rising one; GLP-1 adequacy net silences the protein
teaching a non-GLP-1 twin earns; loud Saturday = weekly shape never a
note; sparse 21-day flat ≠ plateau, dense = honest note; hand strength
stands the resistance note down; near-empty record → NOTHING (I DON'T
KNOW YET as honest emptiness). H=p53 usuals; I=M1 suite; K/L=menses +
p53 stand-downs. ⑦ **§14 BOOK [CORR p50]: BOOK + weigh-in ledger were
ALREADY fullScreenCovers** — no nav change; what was missing was the
tap: Home nutrition instrument → `.plates` (one line, deep-link route),
both ends filmed. **§11 Body Snap re-opened: KEEP verdict stands** (no
BF% from photos, ever; waist-trend = founder gate on the
ObservationKind server CHECK). **§12 water refused again over the 2025
Delphi ≥2L — HF/CKD asymmetry; copy-only stands.** ⑧ **AX5 films
caught the read clipping BOTH edges** (two fixedSize dateline runs
forced the page wider than any phone; JKScreenChrome centered it —
p52's mechanism, new surface) → dateline + signals stack ≥AX1 +
**min-height ScrollView law**; standard-size composition byte-same on
film; **SE film showed the decline door BELOW THE FOLD at DEFAULT
size** — the scroll container was owed to SE, not just AX5. The §12.1
trap re-fired (zsh unquoted $2 → real app → paywall frames) and was
caught by `ps -o args=`. New door: `--debug-read-scroll-bottom`.
⑨ **PROOF: app 1483 · 0 failed · 2 skipped (1485; +37 vs 1448
reconciled EXACTLY: +40 A–F, −13 corpus, +9 adversarial, +1 unit pin —
identity-diffed vs the post-deletion bundle) · PlankFood 242/242 (+3) ·
PlankSync 29/29 · DayKey 3/3 under ar_SA · Release BUILD SUCCEEDED,
0 errors.** 55 source files by `find -newer`; Payment · Paywall · Auth
· AppPhase · Info.plist · entitlements · widgets · BodyScan ·
`supabase/` · EFs **EMPTY**; @Model ×3 zero-diff; RED artifacts
retained. **No SQL, no deploys, no migrations written or applied
(p53's `20260818090000` still WRITTEN NOT APPLIED — gate stands); no
sim erases, standing QA identities reused.** NAMED NOT FIXED: v4.5
OnboardingView (30.25 MB, founder-gated since 48) ·
Signals.BodyLine/CoachSummary.seasonNote/WeightAnalytics.subtitle dead
strings · EF envelope block (deploy-gated; client half live) ·
recordIgnored latent · E-14 threshold divergence · GLP-1 anchor
always-protein monotony. **FOUNDER GATES: p53 migration · EF deploy ·
waist-trend decision · v4.5 sweep · archive-time bump 33→34.**
**P0: 0 · P1: 0 new. NOT ARCHIVED, NOT UPLOADED, NOT SUBMITTED.
SAFE FOR PASS 55: YES.**

## Project status (2026-08-18) — THE ANSWERING RECORD

**THE ANSWERING RECORD (feat/app-v2). BUILT 2026-08-18, after 51/52
(52 THE FIRST DAY has no block here; its record is
`docs/app_v25/52_*`).** `docs/app_v25/53_THE_ANSWERING_RECORD.md` is
the record. 51 was defensive (the record must not become less true);
**53 is the offensive half: the record should ANSWER BACK** — record
once → correct once → Jeni remembers → the product changes → she can
see why. **One defect in four costumes: a fact she gave once that the
product could not give back.** ① **FOOD MEMORY** — her daily latte was
priced fresh by the model every time; `FoodUsuals` (pure fold,
frequency-then-recency, `PlatePriors.normalize` is the ONE title key)
now answers the words door from her own record ("your usual · logged
12 times · with your fixes", exits: count it fresh / use the package),
stamped `.again` because the model was NOT consulted; **a qualifier is
a statement, not a fuzzy match** ("half a turkey sandwich" never
matches the whole one — the §27 law extended); barcode verify-once
re-serves HER fixed row; **every edit is a correction**
(`derivedEditNotes` diffs the session, persists in `payload`, renders
in YOUR NUMBERS; `wasVerified` = corrections OR edits); usuals and
priors never stack by construction. ② **THE GLP-1 REGIMEN GREW UP —
plan and event are separate facts.** Cadence gains **everyN (2–90
days)** and **twiceWeekly** (G1/G2 closed): the interval chain is
EVENT-ANCHORED (taken→takenDay+N, skipped→slotDay+N, late pushes the
next — how the drug is actually taken), `cadenceWord()` is the one
vocabulary every surface/envelope/reminder speaks; ONE quiet layer in
the day editor ("a different rhythm"); **backfill** ("+ add a past
shot" → 14 unrecorded-day chips → the SAME DoseSheet, no second
editor); the late face records WHEN ("on saturday, forgot to log" →
takenAt = that day, re-anchoring from it); **doseLabel** puts the
amount she actually took on the EVENT; **tenure**
(`treatmentStartedOn`, month resolution, "on it since" door) makes
month-4-not-day-4 a stated fact ("month 4 of treatment, by her
account" in the packet). Interval reminders are one-shot re-armed;
weekly anchors gain the second trigger. ③ **MOVEMENT recorded by hand
counts EVERYWHERE** (Becoming tile, WeeklyBodyReview, VisitPacket
strengthSessions7, Jeni read_activity, Method preservation input —
was Home-only) and has a list + delete at last. ④ **[CORR] 51's "one
weight story" was incomplete** — brief/journey/weekly-review/packet
still consumed the fast EMA; all four now read the canonical fold;
the fast fold has exactly ONE consumer left (the band trigger, its
documented calibration exception). **[CORR-2]
`HomeEvening.tomorrowIsDoseDay` compared an Apple weekday to an ISO
anchor** — wrong evening every week, never Sunday; fixed via engine
`isDoseDay` (no seam existed for RED; the rule lived in a view body —
the §36 lesson again). **METHOD**: `EvidenceTier` with **"weak"
deliberately not a case** (unevidenced = unshippable by construction);
2 record-earned notes (morning protein gap from HER shortfall; salty
dinner → "it's water, the trend didn't move" — only speakable because
of the one fold); `settleFollowUps` emits `.methodFollowUp` — **the
JITAI finally learns whether its advice landed.** **JENI KNOWS WHAT
SHE KNOWS**: dated memory ("told 3 days ago"), door words + her
numbers on plates, `treatment_months`, event-aware day_after_dose,
read_weight_trend returns actual recent rows. **CYCLE, smallest true
version**: `CycleService.bootstrap()` had ZERO callers (the Info.plist
promised "cycle timing" for a service that never ran) — wired;
irregularity stand-downs (spread>10d → silence; luteal needs HER OWN
plausible gap); menses-onset water copy only (White 2011); prediction
refused by law. **COHORT GRAMMAR ON THE INSTRUMENT** (founder ask,
researched): donut REJECTED (Cleveland–McGill: angle is the worst
task; part-to-whole is not "can I eat this"); **on-medication days
COUNT UP and never say "over"** (WW-countdown/what-the-hell
literature; GLP-1 skews under-eating) — **caught live on the SE film:
2,010 of 1,473 kcal, silent**; weight-loss keeps "· 1,040 left / ·
200 over". **@Model TOUCHED for the first time since `1710180`** —
additive optionals only, migration PROVEN by opening the pass-52
store (films retained; the QA-sim "lost row" scare was its documented
uid rotation, not schema). **ONE MIGRATION WRITTEN NOT APPLIED**
(`20260818090000` — 5 additive columns); pre-migration a customer who
never touches the new facts syncs exactly as today
(`encodeIfPresent`), one who does fails-and-retries visibly (§45
reporter) and lands after the founder applies. **DESIGN PASS**: 31
films; frame-caught: backfill caption ambiguity;
**JKEmptyState broke "movement" MID-WORD at AX5 — the first fix
(minimumScaleFactor) did NOTHING and the refilm proved it** (with
fixedSize the floor never engages); real fix = decorative line caps at
accessibility2, kit-level, RED→GREEN films retained; entrance
contact-sheet clean. **THE TRAP FIRED TWICE AND WAS CAUGHT TWICE**
(a `tail`-swallowed `Executed` line; a stale-cwd invocation whose
error the grep ate — every final count re-read from xcresult).
**PROOF: app 1446 passed · 0 failed · 2 skipped (total 1448;
+50 vs baseline reconciled EXACTLY: 28+10+10 new + 1 + 1) · PlankFood
239/239 (+14) · PlankSync 29/29 · DayKey 3/3 under ar_SA · Release
BUILD SUCCEEDED.** 61 files by `find -newer`; Payment · Paywall ·
Auth · AppPhase · Info.plist · entitlements · widgets · BodyScan ·
EFs **EMPTY**; RED artifact retained (15 methods / 17 failures,
regimen BEFORE-state). UI walk legs XCTSkip on the QA sim's recorded
identity race — behavior unit-pinned, states filmed via the new
`--uitest-regimen-page` door. **Production mutations: none; +1
anonymous bootstrap account on the film sim (the §45 mechanism,
observed again).** **FOUNDER GATES: apply the migration with/before
this build · the consult cycle question · instrument→THE BOOK
tap-through (proposed).** **P0: 0. NOT ARCHIVED, NOT UPLOADED, NOT
SUBMITTED. SAFE FOR PASS 54: YES.**

## Project status (2026-08-18) — THE RECORD MUST NOT LIE

**THE RECORD MUST NOT LIE (feat/app-v2). SURGERY, BUILT 2026-08-18.
BUILD 32 IS SUPERSEDED** — product source changed, so the frozen tree
stopped being the engineering line; `CURRENT_PROJECT_VERSION` **32 →
33** after the gates. `docs/app_v25/51_THE_RECORD_MUST_NOT_LIE.md` is
the record. One goal: **a user record must never become less true
because Jeni read it, imported it, edited it, synced it, reinstalled
it, or crossed midnight.** Pass 50's four named lies all reproduced
RED and closed: ① **W1** — a weigh-in TYPED over a same-day Apple
Health row kept `"healthkit"` and the importer reverted her number on
the next launch; `persist` now runs the same one-line
`sourceAfterCorrection` law `update` always had. ② **P1** —
`start_date` is a CIVIL DATE that was written as the UTC date and
reparsed as UTC midnight, so every non-UTC reinstall moved the program
day ±1 (LA read **day 6 where she lived day 5**; both DST days
slid); `PlanWireDate` now writes and anchors the LOCAL calendar day,
proven CA/NY/UTC/Tokyo × DST spring/fall × replay-fixed-point, and
walked on the sim across a relaunch. ③ **F2** — every plate edit
erased a grounded item's micronutrients (the FIFTH and SIXTH instances
of the defaulted-init field drop: all three PlateEditSession helpers
AND the editor's save); every CapturedItem/CapturedFood copy path is
now a MUTATION that carries unnamed fields by construction, and
`FieldPreservationTests` pins the 25-field count so the NEXT forgotten
field fails a test before it ships. ④ **F3** — a NULL `food_logs
.source` hydrated as `"photo"`; absence now arrives as absence.
**THE SWEEPS FOUND WORSE, ALL FIXED RED→GREEN:** the pass-43
PRODUCTION plan-archive **could never land** — server `now()`
timestamps carry microseconds, the bare formatter parsed them to nil,
and the merge ADOPTED the nil, silently UN-archiving the plan support
archived, on every hydrate (`WireTimestamp` now parses both shapes at
all seven read sites, including `CareConnectionService.establishedAt`,
which was nil for 100% of rows and let the PHI-destination sort run on
`.distantPast`). `TodayStateService.dayKey` followed the device
locale/calendar while its ten readers were pinned — measured under
`-testLanguage ar -testRegion SA`: both producers minted Islamic-era
keys (`1448-03-05`) that round-tripped to **year 0851**, forking dose
slot ids and zeroing the clinician packet's adherence loop; pinned
Gregorian/ASCII, proven under both locales. `hydrateUser` NULL columns
BLANKED local facts (gender → "", height → nil, promisesKept → 0) and
pushed the erasure back — present-only adoption now, via the new
`applyHydratedUser` seam. Invented provenance at three more hydrates
(weight `?? "manual"`, dose `?? "sheet"`, observation `?? "manual"`) →
`"unknown"`. `exercise_results` was write-only since v1 (per-exercise
breakdowns lost on every reinstall) — decoded back. **ONE WEIGHT
STORY:** new `WeightSeries` (the canonical resolved series: sign-up
self-report excluded, EARLIEST-of-day) + `WeightWeekReadEngine
.trendSeries` (drawn lines share the spoken fold) behind Becoming's
tile (which held a FIFTH hand-ported EMA pass 50 never counted), the
chat card, the delta lines, jeni's tool, InsightEngine, the band
trigger and **VisitPacket** (which had its own fourth universe); the
fast EMA survives ONLY as the internal trigger fold (its safety
thresholds are calibrated to it — the swap is pass 53's "one
smoother", documented). **DELETE STAYS DELETED ACROSS TIME ZONES:**
the day tombstone is a zone reading, so a cleared weigh-in resurrected
after travel — a zone-proof INSTANT tombstone now blocks the same
Health sample everywhere (differential-proven), and the removal note
stopped warning "a later sync can bring it back" (false since §44).
**Visual edges:** D1 (content ghosting under the floating bar) and D2
("your medicatio/n" at SE·AX5) reproduced → repaired (bottom paper
fade + 18pt scroll clearance in `tabRoot`; title wraps-or-scales) →
re-shot on 16/SE/AX5 with a 60fps entrance check; **D6 (ruler "1675")
did NOT reproduce at full resolution and was left untouched.**
**Sim journeys on the real UI, solo:** weigh-in kept → listed as HERS
→ removed → relaunch (hydrate + import run) → **still gone** (63s);
program day identical across relaunch; the words-provenance leg hit
the same QA-harness words-door blocker pass 50 recorded (unit-pinned
instead, stated not scored). **PROOF: 1386/1386 app (+18; 2 skipped =
the standing env-gated pair) · 29/29 PlankSync (+20) · 215/215
PlankFood (+7) · DayKey suite green under BOTH locales · Release BUILD
SUCCEEDED, 0 errors.** 48 files; **@Model files ZERO-DIFF (no store
migration exists to fail); Payment · Paywall · Auth · AppPhase ·
Info.plist · entitlements · widgets · `supabase/` EMPTY**; touched
outside the historical empty-set and named, not smuggled:
NotificationOrchestrator (series swap only), CareConnectionService +
CareWeekSummary (parse/key pins), BodyScanSyncService (parse pin),
PlankAIApp (QA door speaks the real wire vocabulary). **No migration,
no deploy, no production SQL; journey writes net ZERO rows through
shipped paths on the standing QA accounts.** **DOCUMENTED NOT FIXED
(§18, each with a site and an owner):** the regimen/facts
server-authoritative hydrate branches that adopt over an unsent local
write AND clear its retry (an authority-policy question — client
writes to those rows 42501, so a naive guard would freeze adoption);
day_progress's missing retry (needs the one @Model field this pass
refused); weekly_reads' invented offer/decision on NULL + discarded
row.id; insert-only cross-device edits as a stance; G1/G2 → 53;
F1/R1 → 52. **P0: 0 · P1: 6 named. NOT ARCHIVED, NOT UPLOADED, NOT
SUBMITTED. SAFE FOR PASS 52: YES.**

## RELEASE STATUS — BUILD 31 SUPERSEDED · BUILD 32 PREPARED 2026-08-15

**BUILD 31 WAS DELIBERATELY SUPERSEDED.** The founder walked the
onboarding on a physical iPhone before submission and found three
things; the freeze was broken for those three and nothing else.
`docs/app_v25/48_THREE_THINGS_BEFORE_SUBMISSION.md` is the record.
① **THE MEDICATION SCREEN DID NOT SCROLL BECAUSE THE CONSULT HAS NO
SCROLL CONTAINER ANYWHERE** — `grep -rn ScrollView PlankApp/Views/
OnboardingV8/` returns nothing; the transcript is a `VStack` moved by a
manual `.offset`. `medOne` is TEN rows (8 injectables + 2 outs) ≈ 660pt
of options and **has never fit on any iPhone**. **[CORR] the note in
`V8Stage.inputColumn` — "standard inputs always fit", XXL overflow filed
as a follow-up — was wrong at DEFAULT type on a 6.7" phone.** Measured:
the last option's frame read **868.6666666666666 before eight full-screen
swipes and 868.6666666666666 after — 0.0pt**. And **XCUITest reported it
`isHittable` while it sat 50pt BELOW the screen**, which is why four
walkers tapping `ozempic` never caught it. FIXED with ONE `ScrollView`
in both modes (a conditional container would re-insert the question on
every beat), `.scrollDisabled` unless asking AND the **measured** column
overflows — because **four beats are RULERS on a horizontal
`DragGesture(minimumDistance: 1)`** and a scroll container is exactly
what steals that — plus `.scrollBounceBehavior(.basedOnSize)` so short
lists stay immovable, and a measured dock inset. Visual design at rest
unchanged. Proven on **SE · 16 · 17 Pro Max · AX5 on the SE**, and in
the **REAL consult with chrome**, answering with the last option.
② **THE FOOD SNAP SCAN DID NOT ANIMATE BECAUSE `.onChange` CANNOT SEE A
VIEW'S FIRST VALUE** — the demo inserts `SnapDial` with `if phase !=
.pick` at the instant phase becomes `.scanning`, so it is **born**
`isScanning == true` and `traceTo` never left 0. Worse than still: the
brackets are dimmed to 0.30 *because* a trace is meant to be drawing.
**This path has exactly one caller and never worked** — the only other
call site (`PhotoCaptureView`) is the resting aim. Measured: **two
independent launches, frames 0.38s apart inside the reading,
byte-identical md5 `aedc7ae0…`**. FIXED at the source: a pure
`SnapDial.plan(...)` applied from `.onAppear` **and** both change hooks;
born-idle is a no-op, so the camera's resting frame gained nothing.
Reduce Motion keeps its no-trace law but holds **0.8s → 1.4s**, because
a crossfade ate a third of it. **[CORR] my own first motion test PASSED
against the broken build** — it caught the result panel rising past
2.0s; samples are timestamp-bounded now. ③ **ASSET CENSUS: 177 audited,
7 proven dead, 3.73 MB removed.** **The founder's hypothesis was mostly
wrong, which is the point of proving it** — every family he named
(`onb-itgirl-*`, `onb-logo-*`, `onb-movement-*`, `onb-profile-*`,
`onb-v5-demo-*`, `social-*`) is referenced. Channels closed, not
assumed: generated asset symbols (`Image(.name)` — **zero** first-party
uses), bundled JSON (the 42 `jm_hero_*` are named by `manifest_v1.json`),
`Info.plist` (`LaunchBackground`), build settings (`AppIcon`), the
widget (SF Symbols only). **NEGATIVE PROOF: compiled `Assets.car`
181 → 174 named assets, delta exactly the seven, nothing else changed;
123 of 123 literals that named a compiled asset still resolve; broken
references 0.** Catalog 80,186,010 → 76,273,380 B · `Assets.car`
63,805,608 → 61,080,328 B. **NAMED NOT REMOVED: the legacy v4.5
`OnboardingView` (9,645 lines, reachable only via `#if DEBUG` +
`--onboarding-v4`) is the real residue — 18 assets referenced by nothing
else (14.43 MB) + `bodytype-0…5` (15.82 MB) = 30.25 MB of a 76 MB
catalog.** Class C, not D; archaeology, not asset cleanup; measured so
the call can be made on a number. **PROOF: 1368/1368 app (2 skipped,
exactly `46`'s baseline) · 9/9 PlankSync · 208/208 PlankFood (+8 =
exactly `SnapDialTraceTests`) · WallExit PASS · KeepWall 3/3 · Downsell
PASS (`46` had it red) · v8 walk to the wall 308.1s · reviewer journey
284.8s · 6/6 PASS-48 legs on three viewports · Release BUILD SUCCEEDED,
binary 84.3 MB, 0 debug doors with controls firing.** **TWO TRAPS FIRED
AND BOTH WERE CAUGHT: `Executed 0 tests` + `TEST SUCCEEDED`** (the
method lives in `OnboardingV5WalkerUITests`; that file holds nine
classes) **and a RED reviewer-journey walk that was NOT a regression** —
it had been chained after the two paywall legs on the same simulator and
the wall's `smallerStepShown`/`downsellShown` once-flags are
`@AppStorage`, which `--uitest-fresh-onboarding` does not clear; after
`simctl erase` it passed solo in 284.8s against `46`'s 285.5s. **Nothing
was changed to make it pass.** **Six source files** (`find -newer`);
Payment · Paywall · Notifications · Care · BodyScan · `supabase/` ·
widgets · `Info.plist` **EMPTY vs HEAD**; no migration, no deploy, no
production SQL, **zero production mutations**, no `@Model` changed.
`CURRENT_PROJECT_VERSION` **31 → 32** (4 lines, after the gates);
MARKETING_VERSION still 1.2.0. **P0: 0 · P1: 0. SAFE FOR THE PHYSICAL
DEVICE CHECK: YES. SAFE TO ARCHIVE AS 32 AFTER THAT CHECK: YES. NOT
ARCHIVED, NOT UPLOADED, NOT SUBMITTED.** **NEXT (founder's call only):
the v4.5 `OnboardingView` sweep, worth a measured 30.25 MB.**

## RELEASE STATUS — BUILD 31 UPLOADED 2026-08-15

**1.2.0 (31) IS UPLOADED TO APP STORE CONNECT AND PROCESSING.** Delivery
UUID `60934d9b-87d2-4563-82ca-42188a727a23`, accepted 18:49:30Z, Apple
validation **0 errors 0 warnings**, signed `Apple Distribution: Byungsoo
Ko (AK7RQAKLYW)` (cloud-managed), `get-task-allow` **false** on app and
widget, App Store profile with **0** provisioned devices. **ZERO SOURCE
CHANGED** (1573 files hash-identical, verified twice).
**[CORR] on `46`: the distribution export is NOT a founder step** — the
cloud-managed cert is fetchable headlessly, so archive → export →
validate → upload all ran from this machine. **NOT SUBMITTED FOR REVIEW.**
Remaining gates are all human: processing confirmation, TestFlight
install, one reviewer walk, StoreKit purchase/restore/cancel, the
rejection sequence, account deletion — plus **verify the LIVE listing**,
because `docs/app_store_metadata.md` still describes a v1.0.0 at-home
fitness app with a plank form checker (real 2.3 risk, named since `24`).
`docs/app_v25/47_BUILD_31_DISTRIBUTION_HANDOFF.md` is the record.
**P0: 0 · P1: 0. READY TO ADD FOR REVIEW: YES. READY TO SUBMIT: NO.**

## Project status (2026-08-15) — THE RELEASE CANDIDATE · BUILD 31 FROZEN

**THE RELEASE CANDIDATE (feat/app-v2). FROZEN 2026-08-15.**
`docs/app_v25/46_THE_RELEASE_CANDIDATE.md` is the record. A freeze pass,
not an architecture pass: three questions, and **ZERO PRODUCT CODE
CHANGED** (mechanically enumerated with `find -newer` — five files, three
of them UI tests, one a new probe, one the version bump). ① **THE FOUR
UI LEGS ARE FOUR STALE WALKERS AND ONE SIMULATOR. REAL PRODUCT DEFECTS:
0.** All reproduced solo from `simctl erase`, each root-caused to a
*deliberate, recorded* change the walker predates: both `BodyScanProof`
legs pass `--uitest-force-evening` and **v25 E8 gave the evening a
full-screen close ritual** (`HomeView:387`, 0.9s delay) that stands in
front of Home — filmed, so the tools grid is behind it and the gear is
under its dimming overlay; `InAppQAUITests` dismissed sub-screens with a
`back` control and **pass `36` re-pointed Settings › "my pace" onto
`your numbers`, a SHEET with a grabber**, which then covered `coach,
jeni` at {30.7,534.8}; `DownsellSheetUITests` expected a plain X to open
the discounted year, but **`WallExitIntent.next` is tier-matched now**
so a plain X returns `.smallerStep` — **the walker was asserting the
pre-5.6 ladder, i.e. the defect**. **[CORR] on `45`: MoveHealthProof is
NOT "samples the simulator does not have"** — the sim's HealthKit store
is real, the leg seeds it, and the assertion ABOVE the failure passed
(2 strength sessions, yoga refused, through the untouched read path);
only the two QUANTITY types are missing, which is a partial Health
grant, and Move's *"nothing has come through from health today."* is the
E8.1 law working. ② **A TEST PROCESS REACHES PRODUCTION BECAUSE THE APP
HAS ONE BACKEND — AND THE UNIT SUITE DOES IT TOO** (new; `45` had not
isolated it). `AuthService.bootstrap()` signs in anonymously against
production, the DEBUG seeders write under that REAL uid, and the launch
sweep pushes it: **one `auth.users` row per simulator-keychain
lifetime**. Measured +1 account +1 plan for one walker session, the uid
confirmed two ways (production `created_at` 31s after the run; the same
uid 31× in the sim's SwiftData WAL). **17 synthetic accounts created
this pass, 17 removed** via the new guarded
`docs/app_v25/46_probes/reap_test_accounts.sql`; **all 14 production
counters end byte-identical to the 10:15:50Z baseline** and the newest
`auth.users` row is back to `45`'s 09:02:20 walker account. **The
isolation contract is DESIGNED AND DELIBERATELY NOT SHIPPED** — it
touches `AuthService.bootstrap()`, whose header says *"SESSION STORAGE:
LOCKED"*, on archive day; P2, sequenced for `47`. ③ **THE JOURNEY WE
SUBMIT WORKS, IN RELEASE, ON THE BINARY ITSELF.** A new door-free walker
(`testReviewerJourneyReleaseWalk`) ran **against the Release app** — the
installed bundle carries **0** `uitest` strings — from an erased device
through the real consult to the wall, then **every** exit: press 1 → the
one offer, decline → back to plans, press 2 → **stood down**, *see the
plans* → the wall, press 3 → stood down, **relaunch with no arguments**
→ still deterministic, restore present. **PASSED 3×** (284.3s Release ·
285.5s Debug). **THE RELEASE BINARY CANNOT ENTER TEST MODE, PROVEN THREE
WAYS**: every door literal inside `#if DEBUG` (mechanical walk, control
fires); the archived binary 0 for every door/secret marker against **four
controls that fire**; and **behaviourally** — WallExit, which passes in
17s against Debug via `--uitest-wall-spent`, **fails against Release**
because the door is not there. **STOREKIT IS REAL IN RELEASE** (which
cannot mock): the year **$49.99** · quarter **$29.99** · week **$5.99**,
her own projection, and *"renews aug 15, 2027 unless you cancel"*.
**[CORR] on `45`'s guess that Downsell was "the StoreKit path" — it was
the ladder.** **A FIX WAS TRIED, MEASURED AND REVERTED**: extending the
SCOFF sweep to fix the SE turned a passing 852pt walk red, because **a
completed gate advances on its own** so the loop walked onto the next
beat tapping "no"; reverted, with the finding in the comment. **SMALLEST
DEVICE: no interaction blocker** — the SE walk stalls at SCOFF (5 items,
3 fit) and the captured frames prove taps land, the list scrolls both
ways and the pinned CTA is visible; **the automated walk is INCOMPLETE
and is stated as such, not scored as a pass.** **OFFLINE AT LAUNCH:
PASS** — *"couldn't connect… try again"*, no crash, no spinner, no false
success. **`care_weekly_summaries`: DEFER P2** — its only writer returns
early unless the customer holds an active `visitPacket` care connection,
so no customer flow, no Jeni, no clinician output and no target
arithmetic depends on it; and granting INSERT before its FK lands would
create rows that survive account deletion. **PROOF: 1368/1368 app (2
skipped = the env-gated `SpineLiveSyncTests`) · 9/9 PlankSync · 200/200
PlankFood · Release BUILD SUCCEEDED · ARCHIVE SUCCEEDED.** **THE
`Executed 0 tests` TRAP FIRED TWICE AND WAS CAUGHT BOTH TIMES** — once
from a stale bundle, once from a background command reporting exit 0
while the log ended `** TEST BUILD FAILED **` (the shell returned
`echo`'s status). **`CURRENT_PROJECT_VERSION` 30 → 31** (4 lines, after
the gates); MARKETING_VERSION still 1.2.0. **ARCHIVE INSPECTED FROM THE
`.xcarchive`, not DerivedData**: 0 doors, 0 keys, no XCTest framework
embedded, production backend only; `api.anthropic.com` and
`api.elevenlabs.io` are in the binary and **neither can be reached** (no
Info.plist key → `.missingKey`; `VoiceProvider` never instantiated).
**THE ARCHIVE IS DEVELOPMENT-SIGNED — `get-task-allow` is the proof —
so the distribution export and upload remain the founder's step, exactly
as for build 30.** **P0: 0 · P1: 0. SAFE TO UPLOAD: YES. SAFE TO SUBMIT:
YES. THE BUILD IS FROZEN.** **NEXT (`47`, only after the founder
decides): the test-isolation contract with its own RED→GREEN, then
`care_weekly_summaries`'s FK before its grant.**

## Project status (2026-08-15) — MAKE THE SPINE REAL

**MAKE THE SPINE REAL (feat/app-v2). G1 APPLIED TO PRODUCTION
2026-08-15.** `docs/app_v25/45_MAKE_THE_SPINE_REAL.md` is the record.
`44`'s one remaining P1 is CLOSED: **the spine leaves the phone and
comes back, measured in both directions, for an anonymous customer and
a permanent one.** ① **[CORR] on `44`: IT WAS NEVER "ONE MIGRATION
REMEMBERED AND THE NEXT FORGOT."** From `pg_default_acl` — grantor
`postgres`, schema `public`, tables: `anon=Dxtm authenticated=Dxtm
service_role=Dxtm` (TRUNCATE·REFERENCES·TRIGGER·MAINTAIN and **none of
SELECT/INSERT/UPDATE/DELETE**). Every migration here runs as `postgres`,
so **every table it creates in `public` is born with no data API at
all**; the tables that work carry an explicit grant. The defect class is
structural, silent, and mechanically checkable. ② **APPLIED as
`supabase/migrations/20260815090000_v25_e1_spine_grants.sql`**
(sha256 `e9fa9bc7…`, recorded in `schema_migrations`) — **2 statements,
`grant select, insert, update` on each table to `authenticated`, DELETE
deliberately withheld**; proven privilege-only by a checker that matches
each statement against a strict GRANT shape and **rejects the E1 schema
migration as its negative control**. **CUSTOMER DATA DML: NONE.**
Live-verified after: `authenticated` SIUD = T/T/T/**f**, anon and
service_role still nothing, RLS + 4 policies unchanged, and the full
38-table matrix drifted on **exactly two rows**. ③ **G1's own reasoning
was understated in three ways, now in the applied file:** the shipping
UPSERT needs **SELECT** (supabase-swift defaults `returning:
.representation`) and **UPDATE** (`ON CONFLICT DO UPDATE` must plan)
even on the first write; and **`TO authenticated` IS EVERY ANONYMOUS
CUSTOMER** — correct and load-bearing, since 3,426 of 4,293 accounts are
anonymous and `private.transfer_account_rows` already moves both
families from an anonymous source. **No `is_anonymous` restriction
added.** ④ **REAL API PROOF over PostgREST with the publishable key
only: 23/23 before (every call 403·42501, for anonymous AND permanent,
on both tables — PostgREST printed the exact GRANT in its `hint`) and
26/26 after** (own-row 200/201; A cannot read/write/delete B; unfiltered
select returns only own rows; **and `authority='prescribed'` is still
refused — now by the POLICY, a different error from the missing-grant
one, which is the first time that law has ever been testable**).
⑤ **SHIPPING CLIENT PROOF, not a debug path**: `ProgramFactStore.apply`
→ `retryPendingUpserts` → **`pendingUpsert == false`** (cleared only
after `.execute()` returns) → local wipe → `hydrateProgramFacts` /
`hydrateWeeklyReads` → **`ProgramFactStore.headValue` returns
`.int(5150)`** — the resolver `TargetsService` and `AdaptiveStepsEngine`
read. Account B sees nothing of A. ⑥ **HANDOFF PROVEN** against the
deployed `private.transfer_account_rows` in a transaction that aborts by
construction: facts move with ids preserved, **a prescribed fact is
DELETED not re-owned**, the shared weekly-read window collides and **the
destination wins whole**, the source's own window arrives re-prefixed,
`rows_still_src_prefixed=0`. ⑦ **HISTORICAL LOSS: NONE, and it is a
measurement.** 340 `program_fact_changed` events since 2026-08-10 across
66 identities and **340 of 340 carry `environment: debug` +
`is_test_user: true`** — against a **control of 2,492 production events
from 54 people in the same window.** 1.2.0 (30) is accepted by ASC and
not released, so E1's writers have never run in a production build; and
every local row still carries `pendingUpsert = true`, so the launch
sweep pushes the backlog with no repair code. ⑧ **THE SILENT FAILURE IS
FIXED, narrowly:** `SyncFailureClassifier` (pure) + `SyncHealth` (once
per family per reason per day) + a `SyncService.structuralFailureReporter`
hook the package calls from the four spine `catch` blocks. **Silence is
granted only to codes POSITIVELY identified as transient; anything
unrecognised is reported** — `42501` was an unrecognised code once. The
payload is three categoricals; **PostgREST's `hint` (which prints the
GRANT and names the table) never travels.** ⑨ **SAME-DEFECT SCAN: 1.**
`public.care_weekly_summaries` has 3 policies, 0 grants and a live
non-DEBUG launch writer — **P2, LEFT FOR 46 ON PURPOSE**, because it has
**no FK to `auth.users`**, so granting INSERT before the FK lands
creates rows that survive account deletion (the `food-photos` ordering,
one table over). `patient_invitations` looked identical and is not a
defect (DEFINER-only, 17 rows, working). **PROOF: 1368/1368 app (+14,
exactly the tests added; 1368−14 = `44`'s 1354) · 9/9 PlankSync ·
200/200 PlankFood · WallExit walker green solo · Release BUILD SUCCEEDED
· binary 84 MB / 122,857 strings, 0 debug doors, production Supabase
only, 0 secrets.** **RED proven at 10 failures of 12** with a stub
written as the honest BEFORE state; **the 2 that passed are the 2
controls** — a classifier that calls everything transient is
indistinguishable from one correctly quiet, **the refusal lesson for the
TENTH session running.** `sync_structural_failure` **is present in the
Release binary and is meant to be** — that is the whole fix — and it is
the control that makes the sibling zeros readable. **NAMED, UNRESOLVED,
NOT CHASED: four `plankAIUITests` legs fail SOLO** (BodyScan ×2,
Downsell, InAppQA settings walk, MoveHealthProof) on surfaces this pass
never touched; **exonerated by disabling this pass's only launch-path
change and getting an identical result**; their shape matches this
repo's recorded "synthesized XCUI drags can't scroll the iOS 26.2 sim".
**Nine files this session**, mechanically enumerated by `find -newer`;
Payment · Paywall · Auth · AppPhase · Info.plist · entitlements ·
Notifications · Care · BodyScan · Workout · widgets · `Packages/PlankFood`
· Edge Functions · all three `@Model` files **EMPTY this session**.
**PRODUCTION CUSTOMER-ROW MUTATIONS: NONE THAT SURVIVED** — 10 throwaway
identities and their synthetic probe rows, all deleted through the
shipping RPC and the counts re-read to 4293/3426/867 after each of the
three probe runs; the handoff ran inside a transaction that aborts by
construction; **program_facts 0 · weekly_reads 0 · apple 559 · email 308,
byte-identical to `43`/`44`.** **[REPORTED BECAUSE THE COUNT MOVED]
`auth.users` ends at 4294, not 4293** — one anonymous account minted at
`09:02:20Z`, the second the UI walker bundle launched the app, owning
1 profile · 1 plan · 1 weigh-in · 5 food logs · 0 facts · 0 identities.
Not a probe: it is the app's own anonymous bootstrap, i.e. **the
mechanism by which this project's 3,426 anonymous accounts accumulated,
observed directly and dated rather than inferred.** Left in place (no
credential exists for it, and a hand DML on `auth.users` is not this
pass's to make).
`CURRENT_PROJECT_VERSION` still 30. **P0 REMAINING: 0 · P1 REMAINING: 0.
SAFE TO ARCHIVE: YES. SAFE TO SUBMIT: YES.** **NEXT: `46` — the four red
UI legs first, then `care_weekly_summaries`'s FK before its grant.**

## Project status (2026-08-15) — WOULD I SHIP THIS TO 10,000 PEOPLE?

**WOULD I SHIP THIS TO 10,000 PEOPLE? (feat/app-v2). AUDITED
2026-08-15.** `docs/app_v25/44_WOULD_I_SHIP_THIS_TO_10000_PEOPLE.md` is
the record (numbered 44 because 43 was taken by THE REAL DEVICE, written
earlier the same day). The release-candidate adversarial pass over the
fourteen uncommitted passes `30`-`43`. **ONE P0, AND IT WAS THE NEWEST
CAPABILITY IN THE BUILD.** ① **`34` GAVE THE WEIGH-IN A `remove it`, AND
FOR EVERY CUSTOMER WHOSE WEIGHT COMES FROM A SCALE IT UNDID ITSELF ON THE
NEXT LAUNCH.** `BodyMassImportService` re-reads ninety days of Apple
Health at every launch AND on every observer fire and decides **by
calendar day** — *a day with no local row is a day I create one for* —
minting a **FRESH uuid**, which the deletion ledger (which names the id
she removed) can never see. The screen says *"every weigh-in lands here
with its date, and you can fix or remove any of them."* It was not true,
and the resurrected number went back to being the numerator of BOTH daily
targets and a row in the clinician packet. **FIXED** with a day-shaped
tombstone (`DeletionLedger.clearedWeightDayId`, the same
`<uid>-thing-<day>` shape dose and symptom already use, so the merge's
prefix swap and `carry` translate it for free) plus
`BodyMassImportService.importDecision`, a pure per-day rule. **RED proven
at 3 failures of 4 new tests; the control correctly passed.** ② **THE
CENSUS FOUND THE THING THIS PASS WOULD MOST HAVE REGRETTED NOT LOOKING
AT: `public.program_facts` AND `public.weekly_reads` HOLD ZERO ROWS,
BECAUSE `authenticated` HAS NO PRIVILEGES ON EITHER TABLE** — not SELECT,
not INSERT, not UPDATE, not DELETE. **E1 THE SPINE HAS NEVER SYNCED, FOR
ANYONE, SINCE 2026-08-10.** `20260810090000_v25_e1_program_spine.sql`
writes nine `create policy` statements and **zero `grant`s**; its sibling
one day earlier grants on `dose_events` at line 81. Invisible for five
passes because every write is fire-and-forget in a `try?` and every
hydrate 42501s into a `catch` that only prints under DEBUG — no number
moves, no test fails, and the local store answers every read because it
is the only store there is. **[CORR] on `43`: it is not "no SELECT", it
is NO PRIVILEGES — so "0 rows today" does not mean nobody has written
yet, it means nobody ever can.** The clinic side is unaffected
(`care_set_program_fact` is DEFINER). **WRITTEN, NOT APPLIED:**
`docs/app_v25/44_packages/G1_e1_spine_grants.sql`. ③ **`food-photos` DOES
NOT EXIST** (live `storage.buckets`: one row, `body-scans`, 0 objects;
0 objects total). So every plate-photo upload since 2026-07-25 has failed
into an **UNCAPPED** on-disk queue that `flushPendingUploads` re-walked
**sequentially, awaited, at every launch, ahead of the launch hydrate** —
the heaviest real customer carries 58 — and on a reinstalled device
`hydrateMissingPhotos` made up to **200 failing round trips per launch,
twice, forever**. **THE BUCKET IS DELIBERATELY NOT CREATED**: the
deployed `delete_user_account()` is `delete from auth.users where id =
auth.uid()` and nothing else, and `storage.objects` has no FK to
`auth.users`, so creating it before a corrected purge would make every
plate photograph SURVIVE ACCOUNT DELETION (`40` §A1's own ordering; `42`
[CORR-1] found that purge would throw on a zero-row delete). **[CORR] on
`39`/`43`: `AppSync.deleteCurrentAccount`'s comment claims the RPC
"purges the body-scans bucket server-side as the backstop" — IT DOES NOT.
The fourth false comment on a deletion path.** Client half FIXED
(3-consecutive-failure stand-down · once-per-day download sweep that
spends the day only on real work · 500-entry queue cap). ④ **A CLINIC'S
CLINICAL CONFIG CROSSED INTO THE NEXT ACCOUNT** — `careProtocol
.served.v1` was in no sweep and `CareProtocolStore.current` is a
process-lifetime static adopted at cold start BEFORE any network call, so
account B's protein floor, pace ceiling and hydration aim were composed
from a protocol B's clinic never served; offline it never healed at all,
and it survived account deletion. FIXED. ⑤ **`day.intention.*` WAS THE
FIFTH `day.` FAMILY AND THE ONLY ONE NOT SWEPT** — it reaches
`DailyBriefEngine`, so last night's decision arrived in the NEXT
account's morning brief. FIXED. **NAMED NOT FIXED (P2):** six
`ISO8601DateFormatter()` sites cannot parse a server-written
fractional-second timestamp (`"…18.123456+00:00"` → NIL) — latent, because
`phase` travels with `archived_at` in both writers and production holds
**0** archived-with-live-phase plans · `care_weekly_summaries` still
FK-less (**0 rows**) · `users.program_mode`/`goal_direction` **100% NULL,
0 of 2,943** · `private.environment()`+`has_clinical_authority` are
PUBLIC-executable but **proven unreachable** (`PGRST106 · "Only the
following schemas are exposed: public, graphql_public"`) · 8 accounts
hold two live plans today, which Build 31 heals on their next launch.
**THE AUTHORIZATION BOUNDARY HOLDS:** the deployed
`complete_account_handoff(p_source_user_id uuid, p_mode text)` DOES take
a client uid, and it is a **pure narrowing filter applied after** the
server-computed subject set — it cannot widen anything; anonymous
destinations 42501; source anonymity re-checked at use.
`care_log_client_event` is anon-callable and properly defended (19-value
whitelist, regex + length caps, 30/15min rate cap). **PROOF: 1354/1354
app (+8, exactly the tests added; baseline 1346 re-measured not
inherited) · 9/9 PlankSync · 200/200 PlankFood · WallExit walker green ·
Release BUILD SUCCEEDED · binary 87 MB / 124,103 strings, 0 debug doors,
production Supabase only.** **A `strings` ABSENCE IS ONLY EVIDENCE WITH
A CONTROL** — `-weightday-` reads 0 and so do `-dose-`, `-symptom-` and
`deletions.v1.`, which have shipped for passes. Five source files, two
test files; **`@Model` ZERO DIFF vs `1710180`; Payment · Paywall ·
AppPhase · Info.plist · entitlements · Notifications · Care · BodyScan ·
Workout · widgets EMPTY; `supabase/`, Edge Functions, `Packages/` and
`pbxproj` untouched this pass.** `docs/app_v25/release_candidate_census.sql`
is READ ONLY, **mechanically proven** (comments + literals stripped, 28
write keywords matched as whole words → 0 hits; one statement, starts
with `select`) and executed. **ZERO PRODUCTION MUTATIONS. Zero drift vs
`43`: 4293/3426/867, apple 559, email 308 — identical.**
`CURRENT_PROJECT_VERSION` still 30. **P0 REMAINING: 0 · P1 REMAINING: 1
(the grant, founder-gated). SAFE TO ARCHIVE: YES. SAFE TO SUBMIT: YES.**
**NEXT: apply G1 and watch one sign-in produce a non-zero
`count(*) from public.program_facts`; then sequence the storage purge
BEFORE the `food-photos` bucket.**

## Project status (2026-08-15) — THE REAL DEVICE

**THE REAL DEVICE (feat/app-v2). MEASURED 2026-08-15.**
`docs/app_v25/43_THE_REAL_DEVICE.md` is the record. `42`'s one blocker
was the only claim in the series resting on reading rather than running:
**`linkIdentityWithIdToken` had never touched this project's live
GoTrue. It has now, FOUR TIMES, from a physical iPhone14,8 / iOS 26.5.2,
Debug on device against production.** ① **THE BLOCKER IS CLOSED** — the
call was authenticated, routed and answered by `linkIdentityToUser`
ITSELF, which is reached only after `requireAuthentication` accepted the
anonymous bearer token, the provider resolved, the audience was accepted
and the nonce verified; **the whole link path is live-verified up to its
identity lookup.** ② **[CORR] MANUAL LINKING DOES NOT GATE THIS PATH** —
in the deployed router `requireManualLinkingEnabled` wraps only
`/user/identities/authorize` and `DELETE /user/identities/{id}`;
`POST /token?grant_type=id_token` is not under it, so there was never a
setting to change (and the repo's cached `gotrue-version` was **STALE at
v2.191.0** — live is **v2.195.0**; `token_oidc.go`/`identity.go` are
byte-identical between them). ③ **THE ERROR IS THE AMBIGUOUS ONE `40`
PREDICTED FROM SOURCE**: `422` · `x-sb-error-code:
identity_already_exists` · *"Identity is already linked to another
user"*, identical in every run — **the session did NOT change on the
error**, only on the fallback, and **the client reads no code at all**,
which is now confirmed as correct against the live server. ④ **THE
FALLBACK LANDED ON THE SAME APRIL IDENTITY ROW** (its `updated_at` moved,
`identities_apple` stayed **559** — had any link succeeded there would be
560). ⑤ **COMPLETE `{"moved":1,"retired":1}` four times · four receipts,
ALL `completed`, ZERO open, ZERO sources, ZERO digests, ONE distinct
destination · every retired source's `auth.users` row GONE owning ZERO
rows in all fifteen families** — retired by a call made AS THE
DESTINATION, with no credential for the source, on the founder's own
account. **Zero drift: auth.users 4293→4293, anon 3426, permanent 867,
and every ownership family identical.** ⑥ **[CORR] `moved` COUNTS
RECEIPTS, NOT ROWS** (`v_moved := v_moved + 1` per receipt), and these
sources were seconds old, so **ZERO customer rows changed owner** — the
device proves the LINK · REFUSAL · FALLBACK · RECEIPT · AUTHORIZATION ·
RETIREMENT, and **the row transfer stays proven only by `42`'s
production run.** ⑦ **THE DEVICE FOUND A P0 NO HARNESS COULD: A RACE
BETWEEN A 35-SECOND HYDRATE AND A HUMAN THUMB.**
`syncUserDefaultsFromUserRecord` is the LAST of seventeen steps, so a
returning payer has no height/weight/goal/pace for half a minute,
`MainShell` offers **"start my program"**, and **at 06:25:18 a SECOND
LIVE PLAN was minted with `started_at = today` on the founder's real
account.** The tail of that very function already names the outcome.
**[CORR] THE RESTORE WAS NEVER BROKEN** — the phone showed record found,
exact-case matched, `pendingUpsert=false`, every fact present, the one
missing key filled; **three hypotheses died on that line** (uid case,
`pendingUpsert` block, `IdentityMerge`'s profile re-key). **LATE IS THE
WHOLE DEFECT. FIXED IN TWO LINES**: the restore now runs BEFORE any
network call (heals sign-out in ms) and again after the profile+plans
land (heals reinstall after 4 calls, not 17); safe because **every write
it makes is MONOTONE** — flags only to TRUE, and both refusals are
already pinned by `BodyInputsRestoreTests`. ⑦b **AND SCHEDULING WAS NOT
ENOUGH — THE GATE ITSELF WAS THE FLAG.** The founder filed two
screenshots (*"your plan is here · start my program"* and *"day one. one
card a day"*) and they are ONE line: `TodayHost` decided the onramp from
`@AppStorage("programEraEnabled")` **alone**, which the sign-out sweep
removes; **`ProgramService.startProgram` archives the live plan and mints
a new one UNCONDITIONALLY**, so that button is not additive, it is a
**PERMANENT DAY RESET** when the plan has hydrated — **the worse outcome
is the one where the app knows MORE** (his account survived only because
the plan had NOT hydrated, leaving two live plans and a repairable
state); and `DailyBriefEngine` gates the day-one card on `programDay ==
1` **and nothing else**, so the greeting simply followed the plan.
**A LIVE PLAN OUTRANKS A DEVICE FLAG, BECAUSE THE FLAG IS SWEPT AND THE
PLAN IS NOT** — `TodayHost` now also derives from a live plan via a
reactive `@Query`, so there is no window to lose a race in; a new
customer (no plan) and a graduated one (all archived) still get the
onramp. **`startProgram` deliberately NOT changed** (refusing there would
break re-enroll and graduation, its only other callers) — the reachable
path is closed at the gate, the unguarded write is named. **No
"welcome back" copy was invented:** `DailyBriefEngine` already carries
the returning voice at `daysSinceLastOpen >= 4` and `>= 14`; what was
missing was not a sentence but NOT BEING TREATED AS NEW, and that is the
program day. **THE SERVER WAS NEVER AT
FAULT: 780 of 780 permanent profiles hold height AND goal weight.**
**THE CENSUS: sign-out sweeps 94 keys, sign-in restores 29, SIXTY-FIVE
have no restore path** — `program_mode` (21 readers) ·
`onboarding_goal_direction` (16) · `onb_v4_movement_baseline` (13, and it
OUTRANKS the alias that IS restored) · `onboardingAgeRange` (8) ·
`medicalDisclaimerAckAtISO` (6) · `onboardingPaceChoice` · the food
settings; and **`35`'s three keys are now MEASURED not predicted —
`program_mode`/`goal_direction` are still NULL on a real payer's row,
columns applied 2026-07-03 with ZERO writers.** **NAMED NOT FIXED:** two
`onAuthChanged` firings race and one classifies the transition
`op=none` (the correct `.adopt` came from the other; decided by which
Task resumes first) · **`authenticated` has NO SELECT on
`public.program_facts` or `public.weekly_reads`**, so both hydrates
42501 on every sign-in for everyone (0 rows today; a grant, not a
schema change) · **[CORR] `auth.identities.last_sign_in_at` does NOT
move on a real sign-in** — only `updated_at` does, so a future reaper
must use it. **ONE PRODUCTION WRITE, on the founder's instruction:**
`43_probes/W1_archive_duplicate_plan.sql` — one `update`, one row, five
guards, a `DO` block that rolls back unless exactly one live plan
remains; **ARCHIVED not deleted** (`phase='abandoned' + archived_at` is
this model's own vocabulary for a superseded enrollment, and
`ProgramPlanMerge` carries it to the phone), verified `plans_live = 1`,
nothing deleted. **PROOF: 1346/1346 app (EXACTLY the `42` baseline, +0)
· 9/9 PlankSync · 200/200 PlankFood · WallExit green · Release BUILD
SUCCEEDED · binary 85 MB / 122,513 strings, `43-OBS` 0, every
observation string 0, 0 debug doors.** **NO RED→GREEN IS CLAIMED FOR THE
FIX AND NONE WAS MANUFACTURED** — `hydrateAndSync` is private and wraps
17 network calls with no seam, so an ordering test would test a mock;
the proof is the device measurement plus a production row stamped
`06:25:18`. Seven `#if DEBUG` observation sites, all removed and proven
absent from the binary; **never a token, nonce, email, name, Apple
subject or health value — body facts were reported as `true`/`false`.**
All three `@Model` files ZERO DIFF vs `1710180`; Payment · Paywall ·
AppPhase · Notifications · Care · BodyScan · `supabase/migrations` ·
`scripts/` EMPTY. **Three files changed: `Sync/AppSync.swift` (2 lines),
`App/MainShell.swift` (the gate), `Auth/AuthService.swift` (instrumented
then fully reverted). No schema, no migration, no deploy, no RLS/grant/
function/policy touched; the handoff protocol is byte-identical to
`42`.** `CURRENT_PROJECT_VERSION` still 30. **SAFE FOR BUILD 31: YES.
HANDOFF CLOSED: NO — one blocker: anonymous → a NEW Apple identity has
still never run, and it cannot be reached with the founder's Apple ID
because that subject already belongs to account B.** **NEXT: one Apple
ID that has never signed into Jeni, one sign-in, one line of the
console.**

## Project status (2026-08-15) — THE HANDOFF ACTUALLY RUNS

**THE HANDOFF ACTUALLY RUNS (feat/app-v2). E1 APPLIED TO PRODUCTION
2026-08-15.** `docs/app_v25/42_THE_HANDOFF_ACTUALLY_RUNS.md` is the
record. `41` designed the server handoff and deliberately did not write
the client, because the migration had never been executed. **That
caution was right: audited as hostile code against the LIVE catalog,
`41`'s E1 WOULD NEVER HAVE WORKED, NOT ONCE, FOR ANYONE.** ①
**[CORR-1] `storage.objects` carries `protect_objects_delete`, a
`BEFORE DELETE … FOR EACH STATEMENT` trigger raising 42501 unless
`storage.allow_delete_query` is set — and a STATEMENT-level trigger
FIRES ON A ZERO-ROW DELETE.** Proven against production before applying:
a delete on a bucket that does not exist, matching nothing, raised
42501. So `complete_account_handoff` aborted its whole transaction on
every call. **② THE SAME DEFECT IS IN PACKAGE A1, WHERE IT IS WORSE —
`40`'s staged storage purge would have thrown before reaching
`DELETE FROM auth.users`, so "delete my account" would have FAILED FOR
100% OF CUSTOMERS** on the one path Apple requires under 5.1.1(v); it
was one founder action from being applied. **`40`'s A1 MUST NOT BE
APPLIED**; a corrected file is staged at `docs/app_v25/42_packages/`.
③ **[CORR-2] the deterministic-id rewrite lowercased its TAIL and
production has uppercase** (`ObservationKind.rawValue` is camelCase), so
the server would have minted `-foodnoise-` where the client mints
`-foodNoise-` — a SECOND row for one symptom on one day, from the
function whose job is to prevent exactly that. ④ **[CORR-3]
`provider='email'` is an EXFILTRATION path, not the injection residual
`41` named**: BEGIN necessarily precedes the sign-in, so an email
subject is a string nobody verified, and a typo landing on another real
customer's address would move her whole record into his account and
retire hers. **Email removed from the contract.** ⑤ [CORR-4]
`program_day_checks` DOES have a per-user unique key (the server
preserves plan ids, so `41` §24's reasoning was the client's). ⑥
[CORR-5] **`public.coach_messages` was in NO list — the count is
TWENTY-FIVE families, not 24.** ⑦ [CORR-6] the receipt cap was
per-subject only. ⑧ [CORR-7] the profile rule was half-implemented, so a
destination with no profile row (87 of 867) lost her body facts. ⑨
[CORR-8] a same-uid upgrade left a dangling open receipt.
**APPLIED:** `supabase/migrations/20260814120000_v25_e1_account_handoffs.sql`
(SHA-256 `5bf4897e…`, recorded in `schema_migrations`) — **15 statements,
ZERO DML at apply time, 0 of 29 tables drifted.** **VERIFIED** from
`pg_class`/`pg_proc`/`pg_constraint`/`has_*_privilege`: RLS on with 0
policies, no grant to anon/authenticated/service_role, all three
functions `postgres`/DEFINER/`search_path=""`, `authenticated` may
execute the two public entry points and **NOT** `private.transfer_account_rows`.
**ATTACKED: 57 + 27 assertions against the DEPLOYED functions, 0
failures, in transactions that ROLLED BACK** — a permanent caller 42501,
an anonymous destination 42501, naming a victim source moves nothing and
the victim survives, a source that became permanent is skipped not
deleted, an expired receipt is inert, direct INSERT/UPDATE/SELECT/mover
all 42501, direct ownership rewrite 0 rows; **atomicity forced** with an
injected mid-transfer failure (nothing moved, source not retired,
receipt back to `open` = the retry state). **PRODUCTION END TO END over
the real GoTrue + PostgREST surface**, two throwaway accounts created and
deleted: BEGIN 200, anonymous-receive 403, receipt read 403, mover 404
`PGRST202`, permanent BEGIN 403, email 400, **COMPLETE `{"moved":1,
"retired":1}`**, replay `{0,0}` — source `auth.users` row GONE, her
weigh-in under the destination **with its id unchanged**, receipt
`completed` with source and digest NULLED. **Zero drift afterwards.**
**SHIPPED CLIENT:** `AccountHandoff` (digest · `sub` reader · two pure
requests · classifiers · never-throwing calls) · **BEGIN before the
token exchange, COMPLETE after the switch, and
`dischargeOwedHandoffIfNeeded()` at launch — the recovery that takes NO
ARGUMENTS and needs no local state** · **`HandoffIdPolicy`**, the thing
`41` said could not ship first: *minting a fresh id after a server move
duplicates her entire record; keeping one without a server move strands
it*, so `.preserve` is written the moment a receipt exists and is NEVER
downgraded, and it does not force `pendingUpsert` (the server holds the
authoritative copy) · **`DeletionLedger.carry` — [CORR] on `41` §19: a
deletion follows the row it names, and only when the row kept its
name.** **PROOF: 1346/1346 app (+16, exactly `HandoffRuntimeTests`) ·
9/9 PlankSync · 200/200 PlankFood · WallExit green · Release BUILD
SUCCEEDED · binary 86 MB / 124,458 strings, 0 debug doors, and
`transfer_account_rows` + `account_handoffs` appear ZERO times — the
client cannot name the mover or the table.** **RED proven at 10 failures
of 7 of 16 methods**; the 9 that passed are four controls, three
refusal-shaped and two server-contract pins — **the refusal lesson for
the NINTH session running**. All three `@Model` files ZERO DIFF vs
`1710180`; `pbxproj` diff is file references only;
`CURRENT_PROJECT_VERSION` still 30. **SAFE FOR BUILD 31: YES. HANDOFF
CLOSED: NO — one blocker: `linkIdentityWithIdToken` has still never run
against this project's live GoTrue.** **NEXT: the founder's one device
sign-in, then the CORRECTED A1.**

## Project status (2026-08-14) — THE HANDOFF

**THE HANDOFF (feat/app-v2). BUILT 2026-08-14.**
`docs/app_v25/41_THE_HANDOFF.md` is the record. The pass that ATTACKED
`40`'s fix. **`40` is correct and attacking it found five more ways an
account transition goes wrong — three of them one shape: A DECISION THAT
BELONGS TO ONE IDENTITY BEING CARRIED INTO ANOTHER.** ① **THE CARRY
COULD FAIL SILENTLY AND PERMANENTLY, AND ITS MOST LIKELY COLLISION WAS
UNGUARDED** — `DayProgressRecord.compositeKey` is `@Attribute(.unique)`
and the re-key rewrote it with no destination check, the ONE unguarded
unique key in the merge; **the server has the same shape
(`day_progress` PK is `(user_id, program_day)`, read from the live
catalog — the base schema is NOT in this repo)**; both accounts holding
day 1 is the NORMAL case (43 anonymous / 64 permanent accounts hold
them); **SwiftData does not throw — it silently collapses the two rows
and nothing specifies which survives** — and the whole carry is one
context and one `try?` `save()` with the receipt cleared right after.
② **A PRESCRIPTION FOLLOWED THE PERSON** — the deployed RLS refuses a
client insert of a care-team regimen (`authority='self' AND org_id IS
NULL AND source_protocol_id IS NULL`) or a prescribed fact
(`authority <> 'prescribed'`), and the merge re-keyed both: the push
42501s forever, and LOCALLY `activeCareTeamMedicationPlan` returns a
clinic's plan **for an account that clinic never met**. **Production
holds nine care-team regimen rows and ALL NINE are under anonymous
accounts.** ③ **NAMED → NAMED STILL CARRIED HER WORDS** — the isolation
sweep runs on sign-out and account deletion AND NOTHING ELSE, so a
switch left `move.manual.v1`, `day.note.*`/`day.reflection.*` (which
reach Jeni's envelope), `day.sit.*` and the whole `safety_*` family in
place. ④ **SIGNING OUT MID-HANDOFF STRANDED THE ANONYMOUS PERIOD** —
the sweep removed `sync.pendingMergeV1`; identically, it discharged a
CONFIRMED deletion's owed purge. ⑤ **[CORR] `40` §3.4's "the local
store is a superset" IS FALSE** after a reinstall whose hydrate has not
landed — the retirement could delete the only copy. ⑥ **[CORR] REFUSED
≠ REMOVED** — `40`'s profile and consent refusals left both rows on
disk under a uid about to be retired, where "delete my account" never
reached them. ⑦ **[CORR] THE COUNT: 17 customer-owned `@Model` families
of 18 declared** (`ExerciseRecord` is the exercise library), so the
pre-`40` merge dropped TEN not eleven — and `@Model` was never the
inventory: **24 families**, including `public.day_reflections`, a SERVER
table of her evening words in NO merge at all. ⑧ **RLS PROVES THE
TRANSFER BELONGS SERVER-SIDE** — every UPDATE policy is `USING` AND
`WITH CHECK (auth.uid() = user_id)`, so **no bearer token can satisfy
both halves of an ownership change.** **SHIPPED, all client-only:** the
three operations as contracts (`AccountOperation`, positive proof of
anonymity, `.unknown` is never anonymous) · the switch isolation ·
the day-progress guard · the two authority refusals · one live plan +
one live medication head, **destination wins, A's preserved as
history** · the receipt that survives a sweep · the carry that reports
whether it COMMITTED · the retirement's carry gate. **READY, NOT
APPLIED:** `docs/app_v25/41_packages/E1_account_handoffs.sql` — the
receipt table + `begin_account_handoff` + `complete_account_handoff` +
a `private` per-family transfer. **The authorization is NOT a bearer
credential**: A pre-commits, while authenticated, to a SHA-256 of the
destination subject; B proves itself by BEING the account that owns it,
computed server-side from B's own identity rows. **No identity is a
trusted input; nothing secret is persisted on the device; a client that
lies only locks itself out.** One transaction, so **two states and no
ladder**. **PROOF: 1330/1330 app (+22, exactly `HandoffContractTests`) ·
9/9 PlankSync · 200/200 PlankFood · WallExit green · Release BUILD
SUCCEEDED · binary 86 MB / 123,579 strings, 0 debug doors.** **RED
proven at 38 failures of 15 of 22 methods**; the 7 that passed are four
controls and three refusal-shaped — **the refusal lesson for the EIGHTH
session running**. Eleven files, **ZERO under `Packages/`,
`supabase/` or `scripts/`**, `supabase/migrations` EMPTY, all three
`@Model` files zero-diff vs `1710180`. Six read-only probes proven
read-only mechanically before running; **no production row written**.
**SAFE FOR NEXT BUILD: YES.** `CURRENT_PROJECT_VERSION` still 30.
**NEXT: apply E1, verify it read-only, THEN write the client that calls
it — never the other order.**

## Project status (2026-08-14) — THE LAST ORPHAN

**THE LAST ORPHAN (feat/app-v2). BUILT 2026-08-14.**
`docs/app_v25/40_THE_LAST_ORPHAN.md` is the record. The pass that
ATTACKED `39`'s fix instead of trusting it. **`39`'s link is correct
and it was not enough — its fallback rebuilds the orphan factory for a
different population, and the merge it relies on drops ELEVEN OF
EIGHTEEN record families on the floor.** ① **THE FALLBACK IS THE
ORPHAN FACTORY FOR EVERY RETURNING CUSTOMER** — GoTrue refuses
`linkIdentityWithIdToken` with `identity_already_exists` when the
Apple id already belongs to an account (reinstall · second phone ·
anyone coming back); `39` falls back, signs into that account and
abandons the anonymous uid with everything recorded before the tap
still under it. **Read from GoTrue's own source: TWO OPPOSITE
SITUATIONS SHARE ONE ERROR CODE** (`"Identity is already linked"` vs
`"…to another user"`), so **the error is not the signal — THE OUTCOME
IS.** Fixed by `AnonymousAccountRetirement`: at the instant the
session names a different account, the abandoned anonymous account is
deleted with **its own still-in-memory access token** — the one moment
a credential for it will ever exist again. Cannot name a named
account (gate checked first), cannot fail a sign-in, **cannot be
retried** because retrying needs a bearer token this build refuses to
persist. ② **[CORR] `39` §3 RECORDED DOSE/SYMPTOM/FACTS/READS AS
"REKEYED". ONLY `program plan` IS.** `reattributeModelRows` fetched 7
of 18 `@Model` types, so **every shot, symptom, regimen, program fact,
weekly read, jeni memory and chat message stayed keyed to the
abandoned uid ON HER OWN PHONE**, invisible to every `@Query userId`.
Production carries **164 regimen plans · 53 symptoms · 14 doses**
under anonymous uids. `IdentityMerge` carries all eleven — **prefix
swap for the deterministic ids** (`"<uid>-dose-<day>"`) because a
fresh uuid satisfies the RLS invariant and silently breaks
determinism; chain pointers follow; **destination wins an id
collision, content is NEVER compared**; **consent REFUSED** (a grant
made as one identity is not another's answer). ③ **THE MERGE ALSO
FIRED ON NAMED → NAMED** — a cross-account leak reachable from the
re-auth/wall/paywall sheets. ④ **A JUST-LINKED CUSTOMER READ
`.unknown`** — GoTrue returns the user it loaded BEFORE the link, so
`identities` is empty while `app_metadata.providers` says apple;
Apple revocation handling, the deletion sheet's Apple sentence and the
profile re-upsert all stood down for exactly the customers `39` fixed.
⑤ **[CORR] THE 30 OF 308 EMAIL TRANSITIONS ARE TEST ACCOUNTS** — 30 of
30 are `@example.com`, sub-second, 2026-07-29/30, zero profile rows,
zero health rows = `scripts/s5_pilot_proof.py`. **Email conversion
preserves the uid 278/278 — 100%, not 90%.** ⑥ **[CORR] THE REAPER
CANNOT REPAIR ANYTHING THIS YEAR** — `last_sign_in_at` never moves
(68% of accounts have newer real activity; the old script would delete
**3 living customers** today), and with a CORRECT predicate the
project is **107 days old**, so **120 days matches ZERO** and 90 days
matches **56 accounts / 15 profiles / 14 weigh-ins** out of 3,425.
**Prevention is the only lever that moves this year.** ⑦ a live
refresh token is NOT a usable guard — all 3,418 token-holding
anonymous accounts have a live one. **ATTRIBUTION is impossible;
ABANDONMENT is provable; a superseded uid and a dormant-but-installed
uid are indistinguishable** — the four populations are named
separately and never all called "orphans". **SHIPPED, all
client-only, no schema:** the retirement · the merge extension · the
named→named guard · `AuthService.method`'s `app_metadata` fallback ·
**the merge receipt written AT THE SWITCH** (so a death before
`onAuthChanged` no longer strands her rows) · `AccountDeletionIntent`
finally swept (its doc comment claimed it already was — **the third
false comment on a deletion path**). **STAGED, NOT APPLIED**, in
`docs/app_v25/40_packages/`: **A1** the storage purge the deployed RPC
has never had (re-verified from `pg_proc`; `storage.objects` has NO FK
to `auth.users`, 0 objects, `food-photos` **does not exist** — apply
before it does) · **A2** `patient_invitations.accepted_by` →
`SET NULL` (10 rows, all anonymous, all redundant with the cascading
`care_relationships`) · **A3** three `comment on table` decisions ·
**B1/B2** the Apple refresh-token store + `apple-identity` EF
(**blocked on a `.p8`; §17 has the exact founder steps**) · **D**
`care_weekly_summaries` A/B (**0 rows — free to decide, and it has
been for three passes**). **`scripts/reap_abandoned_anon_accounts.sql`
supersedes `cleanup_orphaned_anon_users.sql`, which must never be
run** — dry-run default, counts only, no identities, transactional,
storage first, auth last, step 3 commented out. **PROOF: 1308/1308 app
(+20, exactly `LastOrphanContractTests`) · 9/9 PlankSync · 200/200
PlankFood · WallExit green · Release BUILD SUCCEEDED · binary 86 MB /
123,283 strings, 0 debug doors.** **RED proven at 25 failures of 14
tests of 20**; the 6 that passed are four refusal-shaped tests, one
control and `39`'s own finisher — **the refusal lesson for the SEVENTH
session running**. Fourteen files, **ZERO under `Packages/` or
`supabase/`**, `supabase/migrations` EMPTY, all three `@Model` files
zero-diff vs `1710180`. Five read-only SQL files proven read-only
mechanically before running; **no production row written**.
**SAFE FOR NEXT BUILD: YES.** `CURRENT_PROJECT_VERSION` still 30.
**NEXT: apply Package A, answer §12, create the `.p8`. The reaper is
maintenance, not a repair — run its step 1 monthly and wait.**

## Project status (2026-08-14) — IF I DELETE IT, IT STAYS DELETED

**IF I DELETE IT, IT STAYS DELETED (feat/app-v2). BUILT 2026-08-14.**
`docs/app_v25/38_IF_I_DELETE_IT_IT_STAYS_DELETED.md` is the record; a
DATA OWNERSHIP pass, not a feature. One invariant: **if the customer
deletes something, Jeni must never SILENTLY resurrect it — and deleting
her account must not retain customer-owned data without an explicit,
documented, legally required retention contract.** `37` called deletion
the biggest architectural lie left and pointed at a two-device food
resurrection. Real, reproduced end to end — **and the census found four
holes `37` did not have, two of them bigger.** ① **`move.manual.v1` was
in NO SWEEP AT ALL** — every workout she typed into MOVE survived
*sign-out* (the next account on the phone saw her sessions in the sheet
AND counted them in Home's strength tile) and survived *"delete my
account"* on disk and in every device backup after. It changes no
arithmetic, which is why five passes read past it; it is customer-
authored content, which is why it belongs in the sweep. **FIXED (P0).**
② **A COMPLETE SERVER COPY OF EVERYTHING SHE RECORDED BEFORE SIGNING IN
SURVIVES ACCOUNT DELETION, PERMANENTLY** — anonymous-first + Apple
sign-in mints a NEW uid, `delete_user_account()` scopes to `auth.uid()`,
and `scripts/cleanup_orphaned_anon_users.sql` says so in its own header
while being founder-run, unscheduled and 90-day-windowed. The privacy
policy says *"No soft-delete; the data is unrecoverable."* **P0, NAMED,
needs the census first.** (`AuthService.signInWithApple`'s doc comment
claims the uid is preserved; the entire merge machinery around it says
otherwise.) ③ **Sign in with Apple tokens are NEVER revoked** — no call
to `appleid.apple.com/auth/revoke` anywhere; the RPC header records the
absence as intended on a "clean slate" argument that does not answer
Apple's requirement. **P1 App Store compliance, sized, not built (needs
an EF + the team key).** ④ `care_weekly_summaries` still has no FK and
no delete policy (`37` §16, confirmed from schema). **[CORR] ONLY FOOD
RESURRECTS** — all four hydrates are insert-only, but only food has a
push-back-by-diff (`pushLocalFoodEntriesMissingFromServer`, every
launch, unconditional); weight/dose/symptom leave the server and stay
gone, leaving a GHOST on the other device (and for weight that ghost is
the numerator of both daily targets). **[CORR] `37` §6's consent finding
had the wrong table** — the clinic scope is `visit_packet_view`, created
and revoked SERVER-side and revocable from any phone today; the local
`ConsentGrantRecord` writes `visit_packet_sharing`, which gates nothing
and is read only by its own toggle. The real defect was a display lie +
duplicate grant rows. **[CORR] body scans ARE stated** (`"your scans
live on this iPhone. nowhere else, unless you turn on backup."`).
**SHIPPED, all client-only, no schema:** the `move.manual` sweep · **THE
DELETION LEDGER** (`deletions.v1.<uid>`, UserDefaults, so no `@Model`
and no store migration — record at every delete, `supersede` at the
deterministic-id write chokepoints, sweep after every hydrate,
**cleared on account delete and NEVER at sign-out**, because sign-out
preserves the rows it protects) · `hydrateConsentGrants`. **A SERVER
TOMBSTONE WAS EVALUATED AND DELIBERATELY NOT CHOSEN FIRST: it cannot
ship alone** — build 30's `fetchFoodLogs` has no `deleted_at is null`
filter, so applying it before a filtering client would turn every
deletion into a resurrection ON THE DELETING DEVICE. **Client first,
migration second — the third time this ordering has decided a
sequence** (`31` §21, `37` §8). **DELETE BEATS UPDATE** is now a written
rule. **PROOF: 1273/1273 app (+14, exactly `DeletionContractTests`) ·
9/9 PlankSync · 200/200 PlankFood · WallExit green · Release BUILD
SUCCEEDED · binary 86 MB / 123,631 strings, 0 debug doors.** **RED
proven at 10 failures of 14**; the 4 that passed are two controls and
two refusal-shaped tests — **the refusal lesson for the FIFTH session
running**. Nine files, two new, **`supabase/migrations` EMPTY**, all
three `@Model` files zero-diff vs `1710180`. `Packages/PlankSync` +1
additive method; `Packages/PlankFood` and `supabase/` untouched.
**`docs/app_v25/deletion_audit.sql` is read-only, counts and bounds
only, WRITTEN AND NOT EXECUTED** — Q1 the anonymous-orphan census, Q2
`care_weekly_summaries` orphans (decides whether the FK needs
`not valid` first), Q3 how many customers really run two devices.
**NO migration written to `supabase/migrations`** — the
`care_weekly_summaries` FK is blocked on a **LEGAL** question (is a
weekly clinical series something a covered entity must NOT delete?),
drafted three ways in §10 and left for the founder. **SAFE FOR NEXT
BUILD: YES.** `CURRENT_PROJECT_VERSION` still 30. **NEXT: run
`deletion_audit.sql`; nothing else should be decided first.**

## Project status (2026-08-14) — THE ONE RECORD

**THE ONE RECORD (feat/app-v2). BUILT 2026-08-14.**
`docs/app_v25/37_THE_ONE_RECORD.md` is the record. Not an era. One
question: **does Jeni actually have ONE record of the person?**
**Almost — the arithmetic has one authority and the RECORD did not, in
four places, three of them one shape.** ① **THE COACH WAS ON A SECOND
WEIGHT LADDER** — `CoachContextAssembler` published `current_kg` from
`snapshot.latestWeightKg` (the raw weigh-in row), so a woman who told the
consult her weight and never opened the scale had **NO weight in the
envelope** while `your numbers` drew `124 lb` — *and* `to_go_kg` two
lines below DID resolve through the ladder, so the payload answered "how
far am I" from a number it refused to state. `35` fixed this ladder one
field down and left this one; **every existing envelope test seeds a
weigh-in first**, which is why four passes missed it. ② **THE PACE WORD
HAD THREE AUTHORITIES** (`HomeView`, `JKPlanNumbersSheet`,
`ProgramSetupSubflow`) and the coach got the **raw `medium`** — every
screen says `steady`. `IntensityTier.paceWord` is the one authority now;
`36` agreed the copies by editing one of them, a fix with a half-life.
③ **`the doses` and `the symptoms` sit in ONE FRAME and spoke two day
grammars** — a shot marked this morning read `aug 14` above a symptom
reading `today`; `DoseLedger` was written first and never got the
`today`/`yesterday` branch nor the `f.timeZone` line its two siblings
carry. ④ **THE INJECTION SITE reached NO payload** — on the record since
v24, on the dose sheet, on every ledger row, and `33`+`34` both list
*"where did I inject last?"* as answered by `read_dose_history`, which
returned a **status tally**. Fixed, payload-only. **P0 PRIVACY, the
budget's own exception: `clearLocalUserData` missed `JeniMemoryRecord` ·
`ProgramFactRecord` · `WeeklyReadRecord`** — three families added after
the 2026-08-08 "deletion means deletion" sweep, the first being **free
text she typed** and Settings lists with a per-row *forget*. **NAMED NOT
FIXED:** `public.care_weekly_summaries` has **NO foreign key** and no
delete policy, so a clinic patient's weekly jsonb payloads **orphan
permanently** on account deletion (P0, migration) · **a plate deleted on
one device is RE-UPLOADED by the other on its next launch**
(`pushLocalFoodEntriesMissingFromServer` runs every launch;
`mergeRemote` is insert-only) — no offline step, and worse than `34`'s
recorded limitation; tombstones are the only real fix ·
`consent_grants` has an upsert and **no hydrate**, so a clinic patient
**cannot revoke sharing from a new phone** (corrects `34`'s "EXACT") ·
**[CORR] the four FDA micros are decoded and DROPPED in
`FoodVisionService.map()`** — deploying the EF alone closes nothing, and
the deploy must come **AFTER** a client release, not before (build 30
sends the label hint but has no `PlateShare`, so it would silently
undercount a multi-serving package) · **[CORR] `read_weight_trend`
returns a trend, never a row**, so *"what did I weigh last Tuesday"* is
not answerable · **the jeni-chat allowlist gates NAMES ONLY** —
`sanitizeTools` forwards `description` and `parameters` verbatim, so *a
new PARAMETER on an existing tool needs no deploy* (making "I forgot
dinner yesterday" a one-step no-deploy change, deliberately not built on
a frozen candidate). **REMOVED:** the launch-time decode of the 442 KB
CBT manifest — background work on **every Release launch** for a reader
with zero production call sites (unreachability re-proven from deep
links, notifications, tools, widgets, tests and migrations first; no
content deleted). **Jeni memory investigated in full and NOT built** —
the `jeni_memories` migration is drafted in §4A with RLS, grants,
rollback and ordering; chat transcript recommended **LOCAL ONLY** (and
`coach_messages` deprecated); exact age recommended as
`users.onboarding_age_years`, **refusing `date_of_birth`** as more
identifying than the product needs. **PROOF: 1259/1259 app (+17, exactly
`OneRecordTests`) · 9/9 PlankSync · 200/200 PlankFood · WallExit green ·
Release BUILD SUCCEEDED · binary 86 MB / 123,625 strings, 0 debug
doors.** **RED proven at 17 failures of 15, then 2 of 17.** Baseline
re-measured (1242), not inherited. **Thirteen files, ZERO under
`Packages/` or `supabase/`**, twelve protected paths EMPTY vs
`1710180`, all three `@Model` files zero-diff. A test defect of my own
(228 vs 229 — TDEE rounds before the deficit) and **the product's own
`JeniToolsTests` caught an em-dash I wrote into a tool description**.
**SAFE FOR NEXT BUILD: YES.** `CURRENT_PROJECT_VERSION` still 30.
**NEXT: the read-only check that costs nothing — are
`users.program_mode` / `goal_direction` / `medication_status` applied?
Three rows makes the highest-value remaining fix client-only.**

## Project status (2026-08-14) — THE APP I WOULD TRUST FOR 30 DAYS

**THE APP I WOULD TRUST WITH 30 DAYS OF MY LIFE (feat/app-v2). BUILT
2026-08-14.** `docs/app_v25/36_THE_APP_I_WOULD_TRUST_FOR_30_DAYS.md` is
the record. An audit of TIME (two personas × 30 days) rather than of
screens, then the smallest set of changes the audit earned. **THE BUDGET
WAS DECIDED BY THE AUDIT, NOT BY TASTE:** of `34`'s five domains scoring
below 8, four are blocked behind a founder gate this session may not open
(micros = EF deploy · memory/chat/move sync = migration · Jeni
navigate/change = new tool NAMES = EF deploy). **The other two —
EDITABILITY and GLP-1 UTILITY — are the same defect**, need no schema and
no deploy, and their machinery already existed: `34` closed the
write-only-in-the-past defect for FOOD and WEIGHT and named the other two.
**SHIPPED:** ① **a past dose row is correctable** — `the doses` rows are
Buttons opening `DoseSheet` on their own slot; **no new editor and no new
engine** (`DoseSheet` + `MedicationLog.resolve` have taken a `slotDayKey`
since v24 — what was missing was a TAP TARGET), and it cannot invent a
day because a row exists only for a slot already on file ·
② **`SymptomLedger` + `the symptoms`** — the third instance of one shape
(`33` gave shots a list, `34` gave weigh-ins a list); a DAY is one row,
alphabetical within it because ranking her symptoms is a judgement; 42
symptom×severity combinations swept against a 28-word banned list ·
③ **a symptom on any of the last 14 days, and a clear that actually
clears**. The rule pinning the whole logger to today was **one line
inside a view body** (`where entry.dayKey == today`), now
`SideEffectLog.recorded(on:)` — *a rule inside a view body cannot be
tested, which is why nobody noticed it was a rule*. And
**`SyncService.deleteObservation` was the missing half**: `delete` did
`context.delete` and nothing else while `hydrateObservations` is
insert-only, so a cleared symptom returned on the next pull — **and
`VisitPacket` reads these rows, so it could reach the clinician PDF**.
No migration: `observations_delete_own` + the DELETE grant shipped
2026-07-28 (verified in the migration file). **THREE CONSISTENCY FIXES:**
one pace vocabulary in the paid product (`IntensityTier.label`
`soft/medium/hard` → `gentle/steady/strong`; **2 display-only call
sites**, raw values untouched) · **Settings → "my pace" now edits her
pace** — it opened `EditProfileView`, which sets `workoutLevel`, a
device-local WORKOUT preference, while the real pace editor sat inside
`your numbers`; **[CORR] `33` and `34` both recorded `EditProfileView` as
dead code "superseded by my pace" — it WAS `my pace`**, and the two
screens shared two of three words for two unrelated concepts · two AX5
breaks found by FILMING (`the` / `day` wrapping inside itself in a row I
had just written, and a pre-existing pill running off the screen edge
since E7). **PROOF: 1242/1242 app (+14) · 9/9 PlankSync · 200/200
PlankFood · WallExit green · Release BUILD SUCCEEDED · binary 85.7 MB /
123,475 strings, 0 debug doors.** Baseline re-measured (1228), not
inherited. **RED proven at 18 failures / 9 of 14 tests**; the 4 dose
tests correctly did NOT go red because the engine already worked — a stub
cannot revert a missing button — and the banned-word sweep passed because
a refusal test cannot tell "refused rightly" from "cannot act at all"
(third session running). **TWO DEFECTS FOUND IN MY OWN WORK:** a test
file that broke `ReattributionTests` by leaving 40 seeded weigh-ins in
the shared container (fixed in MY file; the other test was NOT weakened),
and **a film door that filmed the PAYWALL** — `30` §12.1 again, fixed by
building a harness that mounts the sheet alone. **13 files, ONE under
`Packages/`** (`deleteObservation`, additive, unavoidable). All three
`@Model`-declaring files zero-diff vs `1710180` — measured with
`^\s*@Model`, not a substring match — so no store migration exists to
fail. **No migration, no deploy, no production SQL, no data read or
written.** `CURRENT_PROJECT_VERSION` still 30. **SAFE FOR NEXT BUILD:
YES** — a non-GLP-1 account sees one changed word.

## Project status (2026-08-14) — THE SAFETY ANSWER MUST SURVIVE

**THE SAFETY ANSWER MUST SURVIVE THE ACCOUNT (feat/app-v2). BUILT
2026-08-14.** `docs/app_v25/35_THE_SAFETY_ANSWER_MUST_SURVIVE_THE_ACCOUNT.md`
is the record. Not a feature/design/cleanup pass — the one correctness
question the production reconciliation surfaced. **REAL, and
release-blocking on the NEXT build only.** The pre-paywall safety gate is
a LIVE v8 beat (`s_safetyGate`) writing five `@AppStorage` keys with **no
column on `public.users`**; `clearOnboardingUserDefaults` sweeps the
whole `safety_` family by prefix + `program_mode` /
`onboarding_goal_direction` by name (correctly — the most sensitive
cross-account leak class) and **nothing ever put them back**, on sign-in,
reinstall or a new phone. So a pregnancy / eating-pattern / BMI<18.5 hold
fell through every rung of `energyBasis` into **rule 3, added 2026-08-13**
(`29` §4), which re-derives a rate from the loss goal she gave BEFORE the
gate ran: production hands them TDEE by accident, the next build hands
them **a real deficit**, and the numerals return. Six COHORT keys moving a
pace or protein floor (glp1 status/phase · hormonal stage · sleep ·
weight trend · food relationship · stress) were dropped the same way —
and the server had carried all of them since 2026-06-23.
**INVARIANT: an account transition may forget the device; it may never
forget a safety decision.** **FIX, no schema/migration/DTO:**
`AppSync.restoreCohortDefaults` (7 keys `UserRecord` already holds) ·
`TargetsService.resolvedSafetyCap` (the gate's OWN arithmetic re-run for
the two derivable reasons — `under18` from the age band, `bmi_low` from
height+weight — consulted **only** when the stored answer is absent;
pregnancy / ED / insulin are **never inferred**) · **THE DIRECTION RULE**
(`planHoldsWithUnknownDirection`: a plan holding + a loss-shaped profile +
neither direction key ⇒ **no deficit AND no maintenance number**,
`missingEnergyInput == .direction`, the app asks) ·
`GoalWeightStore.setDirection` (two keys, nothing else) · the coach's own
weight ladder fixed (it named `weight` while Home named the real fact).
**`program_mode` is written on EVERY consult branch, so its absence is a
precise account-transition signal — an ordinary customer never reaches
the branch** (golden persona **1,282 before and after**). **"Stop
sweeping `safety_`" was TESTED AND REJECTED** (cannot reach a new phone;
the sweep runs at sign-OUT before the next identity is known);
cross-account isolation passes under the RED stub AND the shipped code.
**NAMED NOT FIXED:** `safety_numeric_suppression` cannot be rebuilt or
guessed (contained — no calorie numeral either) · `med_hypo`'s cap is not
derivable · **`users.program_mode` / `goal_direction` /
`medication_status` ALREADY EXIST (migration 2026-07-03) with ZERO
writers** — verify applied, then it is a client-only change ·
`users.program_status` / `program_intensity_tier` / `program_goal_date`
**proven zero writers, zero readers** (a schema comment and a
`ProgramService` doc comment both claim otherwise) and
`public.coach_messages` is a second table with no client — false
contracts, deprecate then delete. **PROOF: 1228/1228 app (+21) · 9/9
PlankSync · 200/200 PlankFood · WallExit green · Release BUILD SUCCEEDED
· binary 89.6 MB / 123,135 strings, 0 debug doors** (the first `strings`
pass ran against an EMPTY PATH and printed eight zeros — the
`Executed 0 tests` trap in new clothes). **RED proven at 17 failures of
21**; two of the first RED run's failures were MY fixtures, corrected.
**12 files, ZERO under `Packages/`**, every protected path EMPTY vs
`1710180`, all three `@Model` files zero-diff — **no store migration
exists to fail.** Two read-only SQL files written and NOT executed:
`reconcile_21.sql` · `no_target_census.sql` (the prior session's
"≤3" predicate is now a strict UNDER-count; bounds + a named unknown
dimension, never one figure). **No production row read or written.**
**SAFE FOR NEXT BUILD: YES.** `CURRENT_PROJECT_VERSION` still 30.

## Project status (2026-08-14) — THE BORING PRODUCT AUDIT

**THE BORING WEIGHT-LOSS APP (feat/app-v2). BUILT 2026-08-14.**
`docs/app_v25/34_THE_BORING_WEIGHT_LOSS_APP.md` is the record. A product
audit against one standard: *delete every sentence containing INSIGHT ·
PATTERN · JOURNEY · AI · COACHING — is the rest worth paying for?*
**YES, and it was failing on one thing in four places.** Not a feature —
a TENSE. **JENI WAS A WRITE-ONLY RECORD IN THE PAST.** Food stamps
`Date()` at persist AND at relog; `WeightLogWriter.persist` updates
today's row or inserts a new one; `SideEffectLog.record`/`.remove`
default to `dayKey()`; a dose correction reaches only the open slot.
Deleting a plate was the ONE exception in the whole product. So: a
dinner logged at 12:10am lands on tomorrow forever; a lunch remembered
next morning has nowhere to go; a mistyped weigh-in noticed tomorrow is
permanent — **and while it is the freshest row it is the numerator of
BOTH the calorie target and the protein floor.** ① **`WeightLogRecord`
has stored a day and a number since v1, five engines read it, and NO
SCREEN EVER LISTED THE ROWS** — Becoming draws a LINE, and a line is a
trend, not a record (`33`'s dose-ledger finding, in the domain the
product is priced on). `your weigh-ins` (becoming › your record) is the
boring list — `today · 163.6 lb · 2 lb down`, `at sign-up`,
`from health` — and every row taps into the SAME morning ruler to
correct or remove it, **on any day**. **There was no weight delete
anywhere in the product**, and `applyHydratedWeightLogs` is insert-only,
so `SyncService.deleteWeightLog` was the missing half (RLS
`weight_logs_delete_own` + the DELETE grant already shipped — **no
migration**). A correction to a Health row becomes `manual`, because the
importer overwrites `healthkit` rows within 30 days — its own
"manual rows always win their day" rule, used correctly. ② **THE PLATE'S
DAY IS CORRECTABLE** (`setLoggedDay`, 14 days back, never forward, same
id so the photo travels and the cloud row is an UPDATE) — the P1 three
sessions named and deferred, taken from the RECORD side instead of the
camera: log it now, then say when. ③ The coach could not see the repair
screen `31` built: `targets.inputs` + `doors` (zero EF deploy — the
allowlist gates tool NAMES, not payloads). **NEW LAWS:** a day word must
identify exactly one row (the time appears only when a day carries two);
the change is the difference of the two numbers ON SCREEN, never a
smoothed one; a gain is set in the same words as a loss. **RED PROVEN
BEFORE GREEN, twice** — food 5/8 red with `setLoggedDay` stubbed (the
other 3 assert REFUSALS and a stub that refuses everything passes them,
which is why the 5 exist); weight 5 tests red with the writer reverted,
and `testCorrectingTheFreshestWeighInMovesTheDailyTargets` correctly did
NOT fail because the old product could already fix TODAY — the defect
boundary, drawn exactly. A product bug found by a test expectation and
fixed in the product: `dayWord`'s formatter did not inherit the
calendar's time zone, so a row the calendar placed on `aug 11` printed
`aug 10`. **NAMED NOT BUILT (all P1):** a past dose log cannot be
corrected · side effects are today-only · **Jeni's memory does not sync**
(listed in Settings as durable; needs a migration FIRST, the `31` §21
ordering hazard). **DEAD CODE, WITH EVIDENCE:** `supplementPlans` (there
is no supplement feature) · `go(.jeniMethod)` has zero call sites so the
14-lesson corpus is unreachable · `RepEngine` has zero call sites so the
84-day CBT manifest is unreachable **and still parsed on every launch** ·
`EditProfileView` · `StepsBentoTile` · the legacy v4.5 `OnboardingView`.
**PROOF: 1207/1207 app (+25) · 9/9 PlankSync · 200/200 PlankFood (+8) ·
WallExit 5.6 walker green · Release BUILD SUCCEEDED · binary 0
`--uitest` / 0 `--debug` / 0 `debug-weigh-ins`.** Two protected paths
moved, both additive and both unavoidable (`PlankSync` +26 — a delete the
insert-only hydrate undoes is worse than no delete; `PlankFood` +90 — the
date lives in the persister). Payment · Paywall · Auth · AppPhase ·
Info.plist · entitlements · Notifications · Care · BodyScan · Workout ·
widgets · migrations: **empty vs `1710180`**. All 3 `@Model` files
unchanged — no store migration, re-verified. **SAFE FOR NEXT BUILD:
YES.** `CURRENT_PROJECT_VERSION` still 30 — the archive-time bump to 31
still stands. **NEXT: run `docs/app_v25/census.sql`** — unchanged since
`32`, now two sessions overdue, and still the only input that changes
the plan.

## Project status (2026-08-14) — DAILY UTILITY PASS

**THE WEIGHT-LOSS APP I WOULD ACTUALLY KEEP (feat/app-v2). BUILT
2026-08-14.** `docs/app_v25/33_THE_WEIGHT_LOSS_APP_I_WOULD_ACTUALLY_KEEP.md`
is the record. NOT a correctness pass — `32`'s candidate is untouched.
The question was whether Jeni is the best DAILY tool for a normal
weight-loss user and for a GLP-1 user, measured against 165 competitor
frames (Lose It 77 · MeAgain 62 · Shotsy 26, all opened). **The hard
parts were already better; two boring things were missing, one per
persona.** ① **THE APP COMPUTED HER POSITION AND NEVER STATED HER
REMAINDER** — Home read `1,660 of 1,460 kcal` and stopped, so *"can I
eat this?"* required mental arithmetic. The product already owns the
grammar (`PlateAnswerEngine` closes every protein sentence with "18 g to
go") and **the consult's own device demo captions this exact ring
"what's left today"** — a promise, not just an omission. Now
`· 736 left` / `· 200 over` / `· right on it`, riding `31` §8's `·
<word>` suffix. **MAINTENANCE GETS NO REMAINDER, ON PURPOSE** (an
expenditure estimate is not a budget she was handed) — pinned, and
proven RED before GREEN. ② **`DoseEventRecord` has stored a day, a
status and a site since v24 and NO SCREEN EVER LISTED THEM** — "the
record" showed the ERA chain (did my dose change), the rarer question.
`DoseLedger` + `the doses` is the boring list: `aug 6 · 0.5 mg · left
thigh`, each row at the dose she was actually on that week. It reports
and never grades — pinned by a test sweeping every status × reason
against a banned-word list. ③ **THE BOOK's day spread led with a
photograph (~55% of screen) and exiled typed meals to a second list at
the bottom** → `menuRows` promoted to `dayLedger`, every plate, above
the photos. **ONE LIST REPLACED TWO.** ④ `onb_v5_outcome` — the
consult's first substantive question — had ONE reader and it was the
PRE-PURCHASE reveal; now `profile.came_for` in the coach envelope (zero
EF deploy). **FOUNDER STEERED TWICE, MID-BUILD, AND WAS RIGHT BOTH
TIMES**: the plate ledger first went on Home (*"doesn't prioritise the
to-do list"*), then below the checklist (*"separate it to the food log
feature"*) — the second steer produced a better change than the one
proposed, and **Home's diff is now one string**. Regimen home 0.68 →
`.large`, which made its **pre-existing AX5 break mine** (`medica/tion
ozem/pic`, `weekly / . / thursd/ays`) — fixed, all label/value pairs
stack from xxxLarge. **REFUSED:** food text search (the words door is
already cheaper), meal buckets (schema + a second mental model), the
medication-level curve (again), water (third time), streaks, a widget.
**NAMED NOT BUILT — the biggest remaining boring gap: she cannot log
food to a past day** (every capture stamps `Date()`; a write-path change
on a frozen candidate). **PROOF: 1182/1182 app (+18) · 9/9 PlankSync ·
192/192 PlankFood · Release BUILD SUCCEEDED · binary 0 `--uitest` / 0
`--debug`.** Nine files, two new. Payment · Paywall · Auth · AppPhase ·
Info.plist · entitlements · Notifications · Care · BodyScan · Workout ·
widgets: **empty vs `1710180`**; PlankFood/PlankSync/supabase/Analytics:
**empty this session**. Only 3 files in the repo declare a `@Model` and
all 3 are unchanged — no store migration, re-verified not inherited.
**SAFE FOR NEXT BUILD: YES.** `CURRENT_PROJECT_VERSION` still 30 —
`32`'s archive-time bump to 31 still stands.

## Project status (2026-08-14) — RELEASE CANDIDATE

**SHIP THE BORING TRUTH (feat/app-v2). CANDIDATE FROZEN 2026-08-14.**
`docs/app_v25/32_SHIP_THE_BORING_TRUTH.md` is the record;
`docs/app_v25/census.sql` is the read-only production census, **written
and NOT executed**. The release-candidate pass over `29`+`30`+`31`, with
a bias to FREEZE: **exactly one line of product code earned its place**
(P1 — `HardTierGate.lockReason` claimed *"your metabolism is already
running lower"*, a physiological claim about HER body; it had never
rendered because the gate's age was always nil, and `31` made it
reachable, so the change that exposed it had to make it true. Now
attributed: *"the lean-mass guidance favours a slower glide"*).
**THE REAL TEST IS THE UPGRADE BOUNDARY, not a fresh install:**
`UpgradeBoundaryTests` seeds TEN sandboxes in the shape the PRODUCTION
build left them (coherent · Autym · fabricated-goal · corrected-server ·
rich payer · maintenance · kg · at-goal · pending-edit · no-plan) and
runs PULL→MERGE→HEAL over each. **The biggest upgrade risk is absent by
construction: NO `@Model` definition has changed since `1710180`, so
there is no SwiftData migration to fail** — check that first on any
future release pass. **THE GOLDEN CALORIE MATRIX** pins ~23 exact
integers as release fixtures; deriving each by hand first caught two of
my own errors (sedentary does NOT reach 1200 — the floor is
`max(1200, BMR)` = 1232; and a goal edit barely moves the target because
the PACE tier owns the rate). **THE SYMMETRY TEST**: server 110 vs a
dirty local 115 → 115 survives the pull, reaches the server, and a
re-pull hits a FIXED POINT. **PROOF:** 1164/1164 app · 9/9 PlankSync ·
192/192 PlankFood · WallExit 5.6 walker green · live V8 consult walker ·
Release compiles, binary strings-clean (0 `--uitest`/`--debug`). Payment
· Paywall · Auth · AppPhase · Info.plist · entitlements · migrations ·
Notifications · Care · BodyScan · Workout: **zero diff vs production**.
**`Executed 0 tests` + `TEST SUCCEEDED` happened AGAIN** (wrong
`-only-testing` class) — always check expected count == actual count.
**SAFE TO SUBMIT: YES**, after one mechanical step the founder owns:
**build 30 is already accepted by ASC, so the next archive must set
`CURRENT_PROJECT_VERSION = 31`** (not bumped here — not instructed).
**NEXT: run `docs/app_v25/census.sql` — it is the only input that
changes the plan.**

## Project status (2026-08-14)

**THE CUSTOMER MUST NEVER NEED SUPPORT (feat/app-v2). BUILT 2026-08-14.**
`docs/app_v25/31_THE_CUSTOMER_MUST_NEVER_NEED_SUPPORT.md` is the record.
The third boring-truth pass, and the one the customer's own words
contained: **the database was repaired and her phone kept showing 124.**
Both previous passes read the CLIENT; the failure was the RECOVERY
CONTRACT, so support had no working lever at all. **THREE WALLS, each
alone fatal:** `shouldHydrateOnLaunch` fires only when a synced family is
locally EMPTY — a settled payer has none, so **the launch hydrate never
ran for the people paying longest** · `applyHydratedProgramPlans` was
**insert-only** ("data fields stay untouched"), right for append-only
history and wrong for a plan, which is a MUTABLE STATEMENT ABOUT HER BODY
· `restoreBodyDefaults` was **absent-only**, so the repair reached the
local `UserRecord` and died one assignment short of the `@AppStorage` key
every surface reads. **SHIPPED:** `refreshProgramTruth` (two selects,
once a day, **before** `retryPendingUpserts` — the old order pushed
before it pulled) · **`ProgramPlanMerge`** — the merge needs NO new
timestamp because `pendingUpsert` already answers "has this device said
anything the server has not heard": dirty ⇒ keep local (she edited on a
plane), clean ⇒ adopt; a nil server value NEVER deletes a local fact;
adoption is a READ and queues no push; `id`/`userId`/`startDate`/
`createdAt` are never merged · **`ProgramService.activePlan` now uses the
SAME rule as `reconcileLivePlans`** (earliest startDate) — they disagreed,
and `createdAt` on a hydrated row was the hydration instant, so after a
reinstall plan choice was **the order of a `for` loop** (`started_at` is
read back now) · **THE TWO BROKEN PROMISES**: the consult says *"you can
change this anytime"* (sex) and the onramp says *"pick the rhythm, you
can change it later"* (pace) — **neither was possible**; both built, plus
age, into `JKPlanNumbersSheet` (4 rows → 7: weight · height · goal ·
movement · **calorie equation** · **age** · **pace**) · `HardTierGate`
could not read an age at all (`parsedAge` switched on `"18-24"`/`"55+"`,
**a vocabulary nothing writes**) so Hard was locked for everyone with a
lock reason that named no reason · **arrival is maintenance** (a reached
goal published NO number AND asked for a goal she had already given) —
and the naive fix resurrected `29`'s fabrication (`goal==current==start`),
caught by the state matrix inside one run · Home says `· holding` when
the number is a maintenance estimate · `EnergyLedger.ageMidpoint` was a
SECOND `representativeAge` disagreeing at `55plus` and the default.
**REFUSED:** a `users` age column — not on cost (≤35 kcal) but on
ORDERING: the client change 400s until the migration is applied, so it
cannot ride a build that must be safe alone. **1139/1139 app (+36) ·
9/9 PlankSync · 192/192 PlankFood**, each with `** TEST SUCCEEDED **` and
exit 0. Release compiles; binary strings-clean (0 `--uitest`/`--debug`).
`AutymRecoveryTests` proven **RED (13 failures) before GREEN**. Protected
paths empty except `Packages/PlankSync` (the merge cannot live elsewhere)
and `EnergyLedger`. Doors: `--uitest-persona-autym[-repaired]` ·
`--debug-plan-numbers-focus sex|age|pace`. **§13 is a read-only census
SQL, written and NOT executed — run it to size the repair.**

## Project status (2026-08-13)

**THE APP MUST BE RIGHT (feat/app-v2). BUILT 2026-08-13.**
`docs/app_v25/29_THE_APP_MUST_BE_RIGHT.md` is the record. A
correctness/parity/first-use audit triggered by a paying customer:
*"wanted the goal weight at 110. However, the goal is set at 124, my
current weight… it has me in a caloric surplus for my height (5'3")."*
Her causal claim is not established; **her two observations are ONE bug
and it is ours.** `TargetsService.planImpliedRate` returned `0` for
FOUR different facts — she chose maintenance · her plan is not built
yet · her plan is corrupt · she has no goal on file — and `0` means
TDEE, so **every path that loses the goal ALSO silently put a
weight-loss user on maintenance calories**, labelled as her daily
target. **110 CAN BECOME 124 FOUR WAYS:** `assembleData` published an
absent goal AS THE CURRENT WEIGHT · `safeGoalWeightKg = max(0,
weightForBMI(18.5))` INVENTED a goal at the lowest healthy weight for
her height · two views defaulted the key to 60 kg = 132 lb · **THE
RESTORE HOLE** — sign-out sweeps height/weight/goal/sex and hydrate
restores fifteen OTHER keys and not those four, so a returning payer
came back with `heightCm = 0` and **her energy number vanished**.
**SHIPPED:** `EnergyBasis` (`.deficit`/`.maintenance`/**`.unknown` → NO
number**; a missing plan falls back to her own onboarding numbers
through the same calculator the pre-purchase reveal quoted) · **the
number she SEES is the number the math USES** (frame review caught a
plan rendering `124 → 143.3 lb` for a persona who said 110) ·
`PlanSummary` · **the post-purchase screen states her plan** (`124 lb →
110 lb · 14 lb to go · about 17 weeks`, protein floor, energy target,
dose standing) and **ASKS for the goal when it is missing** ·
`JKGoalRitual` + `GoalWeightStore` — **until today no surface could
show or change the goal weight** · the unit she picked in onboarding
now reaches the app (`onb_v5_unit_lb` vs `weightUnit`). **AX5 caught
`124` wrapping to `12`/`4`, and the daily weigh-in truncating the
weight to `12…`** (pre-existing; it had no film door). **REFUSED:**
water again but newly earned (the log is defensible, the TARGET is not
— it needs HealthKit `dietaryWater` + an `Info.plist` purpose string) ·
Body Scan demoted further not deleted (8 users / 90d, but they kept 56
scans; no evidence base for photographic tracking as an intervention).
**1076/1076 (+41).** Release compiles + strings-proven clean. Doors:
`--uitest-persona-customer` · `--uitest-persona-nogoal` ·
`--uitest-persona-home` · `--debug-goal-ritual` · `--debug-weigh-in`.

## Project status (2026-08-12)

**THE PORTION AND THE SOURCE (feat/app-v2). BUILT 2026-08-12.**
`docs/app_v25/27_THE_PORTION_AND_THE_SOURCE.md` is the record. The food
rail re-measured as a product inside the product. **No migration**, zero
diff against the reviewed release in Payment/Paywall/Auth/Sync/
migrations/`AppPhase`/`Info.plist`/`pbxproj`, zero HealthKit read-type
change, zero analytics vocabulary change. **THE SEAM: the pipeline knows
the SIZE OF THE THING and never learns how much of it she ate — and
every surface presents the result as if it did.** Filmed before any
change: a whole 12-inch pizza read `96 g` protein `of 90 g today`, floor
MET with a full bar, 2,200 kcal, verdict "a little over today". The
brief's lead hypothesis was micronutrients; I disagreed on its own
sentence — *a precise nutrient attached to the wrong serving assumption
is still wrong.* Vitamin D is a GAP; this is a WRONG NUMBER with a
confident ring on it. **Eight instances:** the EF has computed
`servings_in_dish` + `is_shareable` since 2026-06-23 (its worked example
says *"the app lets the user say they ate 2 slices"*) and they had
**ZERO READERS**, threaded through 8 copy constructors and dropped at
every render site · the ladder was hard-coded `1/¾/½/¼` clamped
`min(f, 1.0)`, so one slice of an 8-serving pizza was UNREACHABLE ·
`BarcodeRead` prices ONE serving at `confidence: 1.0` · **there is no
label branch in the EF at all** (the hint rides `text` into a prompt
saying "estimate for the WHOLE visible food"; `servings_per_container`
is not in the schema) · `EntryMethod.isPrintedTruth` is written as law
with **zero production call sites** · **`ResultDetailCopy.provenance`,
the FIRST surface she sees, returned "estimated from the photo" with no
door branch — the exact lie E8.1 was named for killing, one surface
upstream of where E8.1 killed it** · `read_food_day` returned
title/at/kcal/protein only, so *"was my lunch high in sodium?"* and
*"what did i correct yesterday?"* were unanswerable **from a record
holding the answer** · `applyPriors` has ONE call site (photo), so E7's
front door has no memory. **SHIPPED:** `PlateShare` — the ladder comes
from the DISH, the common plate is untouched (trigger is the model's own
`is_shareable`), the number is never changed silently (the plate just
SAYS what it is of), packaged food counts UP with
`servings_per_container` derived FREE from Open Food Facts data the
barcode door already parsed · `NutritionSource.labelDeclared` stamped at
the dispatcher chokepoint · `EntryMethod.provenanceLine` moved to the
type that owns the doors so a door cannot ship without a sentence ·
**Jeni now reads items, sodium, sugar, carbs, fat, the door and HER OWN
CORRECTION WORDS** (zero EF deploy — the allowlist gates tool NAMES, not
payloads) · the copy-constructor bug class killed (`withId` dropped
`micros` — 4th time a defaulted-parameter init lost a field).
**THE CONSENT SCREEN** (founder steered twice: *"needs a major
redesign"*, then *"is that screen fundamentally needed?"*): kept —
5.1.2(i) requires consent before a photo reaches a third party — but
rebuilt as a **FIRST-RUN PRIMER**, three teachings lifted from failure
modes the vision model names in its OWN prompt, then the disclosure,
then accept. It said everything twice before. **FRAME REVIEW CAUGHT:**
the consent card painted **every glyph twice** (`.shadow` applies per
primitive without `compositingGroup`) · the share note sat below the
fold, arriving after every number it qualified · at AX5 the dish title
and stepper truncated each other · the chip row could not wrap.
**1009/1009 app + 187/187 package** (+33). Release compiles. Doors:
`--uitest-open-camera` · `--food-debug-shared`. **FOUNDER GATE — THE EF
IS WRITTEN AND NOT DEPLOYED:** `supabase functions deploy food-vision
--no-verify-jwt` adds the label branch + serving semantics + the four
FDA micros; compatible BOTH ways, strict mode verified programmatically,
`deno check` shows the identical 12 pre-existing errors as HEAD.
**A CORRECTION MADE WITHIN THE SESSION:** I first called the words
door's missing prior the biggest remaining seam. **Wrong** — the
one-call-site is deliberate and the DOC COMMENT ("photo + describe")
was the defect; I trusted it over the engine's law while writing a
document about that exact failure. `PlatePriors` keys on dish TITLE +
uniform scale: on a photo the portion came from the MODEL so a prior
corrects its sizing; through words the portion came from HER, and
**"half a turkey sandwich" normalizes to the whole one's key** and
scales her half UP, past a ±3× clamp blind to 2×. *A prior must never
overrule a portion the user stated herself.* Shipped the argument +
`PlatePriorsWordsDoorTests` instead of the feature.
**NEXT: deploy the EF (founder gate), then THE FOOD BOOK** — the
brief's question this pass did not reach.

**WHAT THE RECORD KEEPS — THE LAST 10% (feat/app-v2). BUILT 2026-08-12.**
`docs/app_v25/26_WHAT_THE_RECORD_KEEPS.md` is the record. Not an era, no
E-number: a depth pass over the layers reached by going further in.
**No migration** (none needed), no paywall/pricing/entitlement/`AppPhase`
/auth/RevenueCat/Supabase diff, **zero HealthKit read-type or purpose-
string change**, zero new analytics events. **THE SEAM, one shape in five
costumes: a layer knows something and does not say it, or says something
it does not know.** ① **HER CORRECTIONS WERE WRITE-ONLY** — E4's
fix-with-words sentences ride the JSONL AND `food_logs.payload` and
`FoodLogEntry` had no field for them, so the plate said "read from your
photo · ranges, not exact" the morning after she fixed it. Now
`corrections`/`wasCorrected` + a **YOUR NUMBERS** tier (her words, in the
transcript's DMSans + `jeweledRose` — serif italic was my error, caught
by the chat's recorded founder ruling), `relog` + `reattributeEntries`
carry them (that call site has dropped a new field **3 times**).
② **MICRONUTRIENTS REFUSED ON EVIDENCE** — one source (USDA FDC), reached
only for items the model flagged <0.5 confidence or couldn't price;
`llm_direct` (the default), OpenFoodFacts, `canonical_pantry` and the
label door publish NONE, so micros exist *precisely where the plate
deserves least confidence*. NOT persisted. The live panel was the bug:
`namedMicros` summed partial coverage and labelled it the PLATE's → now
gated on the whole plate being grounded (`publishesMicros`). **The QA
harness hid it** (`mockItems` attaches micros to `.llmDirect`, which is
impossible). ③ **MOVE** — the "dashed divider" **does not exist** (it is
seven below-half rhythm dashes; circles now); `0 of 2` in 44pt under
"what your body did" → **a count is a hero only when there is something
to count**; "twice a week" was said twice; **`steps 0 · from health` was
an absence in a sensor's clothes**; the last rose primary button went
ink; an underlined text link → hairline capsule; `MoveRecord.isEmpty`
finally reached a screen; provenance didn't scale. ④ **THE DETENT**
(founder mid-session: "3/4 or almost full screen") — Move sat on
`tallFixed` 0.68 with the grabber hidden, so it opened already scrolled
with no way to expand; **five items fit at AX5**. Now `.large`. Dose +
side-effect sheets walked at `.tall` and fine — Move was the one wrong
pick. ⑤ **THE DESK'S PROOF EXPIRED EVERY MIDNIGHT** (E6 scoped proof to
TODAY, the window most likely empty at open) → yesterday + record-depth
rungs; and the "what did i eat yesterday?" starter was **inverted**. The
void is ~23%, not 40%, and was never the defect. ⑥ **`JKSheetChrome`**
header text had no `fixedSize(vertical:)` → lost the height competition
to a flexible ScrollView and cut a dish name with two thirds of the sheet
empty below it. Not an XXXL issue. **1009/1009 app + 154/154 package**
(+14 tests). Doors: `--uitest-plate-corrected` ·
`--uitest-food-yesterday-only`. **Next build's best food work, named not
smuggled: the food-vision schema should read the four FDA-mandated label
micros (vitamin D · calcium · iron · potassium) — needs an EF deploy.**

**APP v25 E9 — THE COHERENCE PASS (feat/app-v2). BUILT 2026-08-12.**
`docs/app_v25/25_E9_THE_COHERENCE.md` is the record. A product +
design sweep on top of the release build; **no migration, no paywall/
pricing/entitlement/AppPhase/auth change, analytics additive only.**
The thesis, from walking the running app first: **nutrition was the
only domain that never became an INSTRUMENT** — everything else has a
shape, nutrition had one ring and rows of equal-weight numbers
everywhere else. Principle: **two nutrients earn a shape, everything
else earns a place.** FOUR THINGS THE RECORD HAD WRONG: E6's "the
three food entrances converge on one reading" is FALSE
(`PlateDetailSheet` is a fourth, oldest reading, still leading with
`340 calories` against the product's own §9 law) · **hydration already
shipped and shipped wrong** · the garish plates in THE BOOK were the
**QA seeder** (0.32-0.55 saturation over the full hue wheel — two eras
reviewed those surfaces against banned colours) · "`--uitest-open-
method` presents but does not render" was the engine correctly
returning SILENCE. **HOME: the five-face carousel is GONE** (its four
trailing faces each duplicated the lead face's own tiers) → one
composed instrument: THE FLOOR (protein's ring) → THE DAY (energy as
ONE shape, kcal stated once) → THE REST (fiber · sugar · sodium,
aligned). ~280pt not ~750; the to-do list is above the fold; the shear
bug class is gone by construction. **THE PLATE READING is protein-
first** in the same tiers via the same kit object. **`JeniRing` phase
fix** (gradient `angle: -90` AND shape `.rotationEffect(-90)` → the
ramp sat a quarter turn behind its own arc; hard seam at a met floor).
**HYDRATION: the number left, the reason stayed** — `jenifit.default`
(the CONSUMER protocol) hardcoded 1,800 ml from ASMBS *post-bariatric*
guidance; no credible body prescribes a personal fluid volume and
restriction is standard in HF/CKD/hyponatremia → `Int?`, nil by
default, a care team's aim renders attributed. **NOT built:** a water
tracker (near-null evidence for this cohort) or the HealthKit
`dietaryWater` read (deferred — needs a new type in a purpose string
that shipped one build ago). **METHOD 13 → 15 notes**
(`fluidsOnAQueasyDay` · `constipationWithLowFiber`), both on her own
record, both forbidden a volume by test; E8.1's architecture re-read
and NOT re-litigated; B2B unchanged. **XXXL caught four breaks** (the
greeting stranded its comma; the day tier + legend truncated; the
tools grid truncated every title) — all fixed, SE clean.
**1002/1002 app + 140/140 package.** Correction for future sessions:
437 `.font(.custom(_:size:))` calls without `relativeTo:` are NOT
Dynamic Type bugs (`.custom(_:size:)` scales); and the sim argument is
`content_size`, not E8.1's recorded `content-size`.

**RELEASE PROOF (2026-08-12). 1.2.0 (30) UPLOADED TO APP STORE CONNECT.**
STATE.md §0.-15 is the record. Zero product code changed. Archive →
Cloud-Managed-Distribution export → **UPLOAD ACCEPTED 00:12 PT**
(build 30 was new to ASC — a duplicate would have been refused).
Release binary proven clean by `strings`: 0 `--uitest` / 0 `--debug`
/ 0 demo-backend; prod Supabase + PostHog only. Release sim walk:
wall resolves live prices + restore + X; no launch flash; XXXL wraps.
Analytics trust boundary frozen in
**`docs/app_v25/24_MEASUREMENT_CONTRACT.md`** (binding): production
data = `environment='production'` ∧ no `is_test_user` ∧
`app_version ≥ '1.2.0 (30)'`; 10 questions, min-n gates, no era
decision before n≥100 payers or 6 weeks. The 5.6 fix
(`WallExitIntent`, total, one-offer-then-stand-down) rides this
build. **Founder residue: ASC processing→TestFlight watch · one
TestFlight event reads `environment: testflight` · ~12-min device
acceptance test · verify the LIVE listing against the current product
(repo metadata doc is a v1.0.0 fitness draft — real 2.3 risk) ·
create/attach 1.2.0 + submit with 5.6-fix review note. NEXT SESSION:
WAIT FOR DATA — no E9 without the contract's first clean read.**

**THE SHIP (feat/app-v2 → origin/main). PUSHED 2026-08-11.**
STATE.md §0.-14 is the record. The convergence session: no new era, no
new retention theory. Re-measured: **three E8.2 founder gates were
already closed** (14/14 migrations applied remote; jeni-chat deployed
byte-identical; food-vision deployed same day) — stale-gate lesson
again. Closed: the HealthKit purpose string finally names
workouts/distance/active energy (the ask shipped in E8.2, the consent
sentence didn't) · the evening close was UNMEASURED (zero events on the
release's central loop) → `evening_close_shown` + `evening_intention_set`
+ `has_intention` on the morning read, carried by the RENDER not the
stored key · BuildChannel audited clean against the StoreKit-environment
confusion (receipt FILENAME = distribution channel; purchases never
consulted). Walked 26 states; 7 walk-caught fixes, all re-filmed: the
hero carousel MEASURES its faces now (the fixed height had sheared
three different rows across three eras) · one-column strip at XXXL ·
the paper fade under the status bar · method note at optical centre ·
18 letter run-ons → sentences · one gram grammar ("76 g") across all
food surfaces · protein-forward evergreen chips. B2B walked: "assigned
by your care team. you record what you took." **999/999 app + 140/140
package + MoveHealthProof + SayItWalk 4/4 solo; unsigned Release
archive compiles.** Hard paywall untouched; `e5.firstPlate.enabled`
still false. **Founder residue (all of it): archive/upload 1.2.0 (30)
· confirm `environment: testflight` on one TestFlight event · device
walk (Move rows from a real watch + the words path once).**

**APP v25 E8.1 — THE LEGACY SURFACES (feat/app-v2). BUILT 2026-08-11.**
`docs/app_v25/23_E8_1_THE_LEGACY_SURFACES.md` is the record. Two jobs:
close E8's two named ship-blockers, then bring Method, Breathwork and
Workout into the current product. **The hard paywall stays** (empty diff
over all six paywall/payment/entitlement paths; `e5.firstPlate.enabled`
still false). **FOOD SOURCE**: `CaptureSource` DELETED, `EntryMethod` is
the one vocabulary in the row + the event + the plate; migration
`20260811120000` widens the CHECK by definition-scan not by name (a
name-only drop would silently add a second contradictory constraint);
**no shipped value renamed**; tested through the real chain — 14
migrations clean and replayed 3× on a scratch Postgres 17, 12 values
accepted, 4 refused, history intact. The lie cost more than analytics:
**PlateDetailSheet told a user her TYPED plate was "read from your
photo"**, the "dining out" title branch was dead, relog inherited a photo
it never kept, and **the chooser's again door fired NO `food_log_saved`**
so every food funnel undercounted the cheapest door. **PROTEIN CLOSE
FILMED** via `--uitest-protein-gap N`, which works BACKWARDS from the
floor so the branch is exact for any formula; E8 named two hour sources,
**there were three** (`HomeView.greeting` read the wall clock) →
`AppClock`. **METHOD: a curriculum became a JITAI.** 84 day-indexed
lessons could only ever deliver lessons 1-2 to a 2.0-day median payer
(464 openers/90d, 23.5% payers, 1.66 days each, 39 lifetime completions).
Evidence-led: **Koh et al 2025 (JMIR 27:e76625), 35 JITAI weight studies
— educational information used in 5; prompts 33, feedback 24; 68.6%
rule-based.** 13 notes, each with WHO/WHEN/WHY/AFTER/QUIET + a required
action; rejected domains recorded with reasons; **`RepEngine` already
held ten good interventions keyed to `canonicalDay`** — the content
existed, it fired on a calendar. **Silence is a return value; no
fallback.** B2B: a clinician's note IS `MethodNote` with
`authority = .careTeam`; override/add/suppress/expire/end built + tested;
attribution is a word plus a drawn mark, never colour. One Jeni via
`method_told`/`method_now` in the envelope (zero deploy). **BREATHWORK
survives** on the craving/impulsivity RCT (Complementary Medicine
Research 2024;31(4):376); the **female-presenting photograph deleted**,
the cortisol-holds-fat chain gone, the glossy heart retired. **MOVE**:
`MovementService` read four HealthKit signals and dropped all four;
strength is the headline (lean mass = 25-39% of the loss); **energy is
measured or absent**, the only estimate is MET-based from her own entry,
labelled, never written back to HealthKit; the workout library is NOT
retired but now has a measurable retirement trigger; `EnergyLedger
.bmrFemale` hardcoded the female constant under "the cohort is
exclusively women" — a unisex defect in ARITHMETIC, and dead. **THE
RESTING NUTRITION STRIP REDESIGNED** (founder: functional but not
minimal): each cell was ONE serif string so "420 of 2,300 mg" made
**sodium the loudest thing on the screen**; a cell is PARTS now, values
right-align down one edge, `dv` rows get full width. **997/997 app +
140/140 package.** Walk-caught: three hour sources, the photograph and
its void, Move unpadded, "3 of 2", a hardcoded "two", the cover racing
the snapshot, once-ever notes filmable once per sim, XXXL truncation, the
strip shearing twice — and an XXXL check I nearly claimed without doing
(`content-size` vs `content_size`). **Founder gates**: `supabase db push`
(**FIVE** migrations now) · `git merge --ff-only` + push · archive/
TestFlight 1.2.0 (30) · **confirm `environment: testflight`** · device
walk of the words path AND Move against real HealthKit. **Open debt**:
Move's HealthKit rows unverified on device · the
medication-discontinuation note is the highest-value content in the
domain and unwritten because the signal does not exist ·
`--uitest-open-method` presents but does not render (`--debug-method-note`
does) · `EnergyLedger.spentKcal`/`isLighterDay` dead · the old 84-lesson
corpus unreachable but bundled.

**APP v25 E8 — THE MERGE (feat/app-v2). BUILT 2026-08-11.**
`docs/app_v25/22_E8_MERGE_AND_LEDGER.md` is the reconstructed state +
the release ledger. Convergence, not a feature era. **The hard paywall
stays** (empty diff over all six paywall/payment/entitlement paths;
`e5.firstPlate.enabled` still false). **THE BRANCH STATE, MEASURED:**
"475 commits" was wrong and so was the ref — local `main` was a stale
pointer **343 commits behind origin/main**. Real figure vs origin/main:
**149 commits, 427 files, clean FAST-FORWARD** (conflicts structurally
impossible). Local main reconciled; the push is a founder gate.
**TWO GATES WERE ALREADY CLOSED** — `jeni-chat` deployed is
byte-identical to local, `food-vision` differs by one code comment;
E3-E7 each re-recorded a gate nobody re-measured. **TWO MIGRATIONS WERE
NEVER LISTED** — four unapplied, not two (`20260804090000` p6 +
`20260811090000` care RPCs join v24's and E1's; lexicographic =
dependency order). **WHY PRODUCTION COULD NOT ANSWER ANYTHING** (E6
called it a fact of nature; it was three defects): TestFlight compiles
as RELEASE so every internal tester sat inside every production funnel
→ `BuildChannel` resolves debug/testflight/production at runtime from
the receipt · E7's own falsification test was unmeasurable because ONE
shared decoder stamps `source: photo` for a photograph, a nutrition
label AND a typed sentence → `EntryMethod` on `food_log_saved` · the
words path fired NOTHING, so every scan funnel excluded E7's own door.
History cannot be reclassified; no attempt made. **HOME: PROTEIN
LEADS** (the §9 law was still inverted on the most-seen surface; E7
deferred it citing carousel deep-links — nothing deep-links into it).
Founder steers: floor bar → **ring** · **resting nutrition strip**
(kcal + macros + fiber/sugar/sodium, no paging) · **steps always stand**
in the to-do list as an offered row (excluded from `actionableBeats`,
never debt) · denominators **only where one genuinely exists** — kcal is
hers, fiber/sodium quote the FDA DV marked `dv`, carbs/fat none, and
**sugar deliberately none** (the FDA limit is on *added* sugars; this is
*total*). **THE EVENING CLOSE, TWICE**: "that's the day, maya." became
her record + tomorrow's reason (`EveningCloseEngine`), every tappable
word became a rose chip; then an expert review the founder asked for
found the screen **made seven asks and paid out once, always on day
N+1** on a base whose median payer lives 2.0 days → **THE PROTEIN
CLOSE** ("there is still time tonight. 18 g would close it…") with a
door into E7's describe path, the only line that can change TODAY and
the clearest case for a number a GLP-1 user cannot feel; `sitAck` moved
off "tomorrow". NOT done (settled decisions, founder's call): cutting
the day-number hero + the nightly trend row. **UNISEX**: the first 48
hours are already clean; fixed the `voicePlaybook` whose north star was
"the smartest **girlfriend** you've ever had" (lesson content
byte-identical, verified); the 64/84 figure conflates **22**
reader-assumed-female · **39** third-person · **3** study populations
(preserve). **942/942 app + 133/133 package.** Walk-caught: the protein
face left ~120pt of void once it led; the strip sheared its second row.
My own tests lied twice — the exact E7 §5.4 trap. **Founder gates**:
`supabase db push` (4 migrations, in order) · `git merge --ff-only` +
push · archive/TestFlight 1.2.0 (30) · device walk of the words path ·
**confirm TestFlight now stamps `environment: testflight`** (if it does
not, every cohort read is still contaminated). **Open debt**:
`food_logs.source` still lies in the synced record · pre-E8 analytics
can never be de-contaminated · the Method corpus' 22 lessons · the
protein-close gap branches are test-pinned but unfilmed.

**APP v25 E7 — SAY IT (feat/app-v2). BUILT 2026-08-11.**
`docs/app_v25/20_E7_DECISION.md` is the why + the hypothesis (written
before the build); `21_E7_EVIDENCE.md` the record. Founder decisions
obeyed: **the hard paywall stays** (`e5.firstPlate.enabled` false,
paywall/pricing/tiers/downsells/entitlement/AppPhase **untouched** —
verified by an empty diff), and the redesign continues.
**The era: putting something into the record must cost one sentence,
and the record must answer in the same breath.** One defect wearing
three costumes, all found by walking: the only wide door to the record
was a CAMERA (the cheap words path already existed, buried behind a
"snap instead" link inside the lens; 3.4% ever start a scan) · the
reading led with CALORIES against the product's own law
(`00_THE_SYSTEM` §9 "protein floor + fiber lead; kcal quiet", from
§7.6's finding that protein 1.2-2.0 g/kg is one of exactly two proven
GLP-1 pillars) · and nothing answered back at the moment a record
landed, on a branch where every intelligence pays out on day N+1 and
the payer median is 2.0 active days.
THE DOOR IS WORDS (`ScanChooser` asks "what did you eat?" over a field
with today's protein under it; E5's two big doors survive at full size
— the founder compared a pill-based cut and kept E5's; `Route
.foodDescribe(spoken:)` sends her own return key straight to the
estimate, jeni's prefill still waits) → PROTEIN LEADS (full width, the
floor as denominator; the kcal ring DELETED) → THE ANSWER
(`PlateAnswerEngine`, pure, 10-case honesty table over the SAME
`TodayStateService`/`TargetsService` inputs; the grid MORPHS into one
sentence then files; no floor on file → no denominator, ever).
**Founder steers taken mid-build**: side-effect rows → pill cloud ·
`JeniSheetHeight` token (`.medium` was HALF a screen) across 15 call
sites · a donut replacing three bars + the split bar · fiber/sugar/
sodium visible at rest · **vitamins + minerals, which `USDAClient` had
parsed since v1.0.9 and `CalorieMathService.compute` silently dropped**
(carried through now; zero means UNKNOWN and renders nothing).
**890/890 app + 125/125 package + 4/4 walk.** New doors:
`--uitest-file-plate`. Also fixed: the steps ring's **Metal shader**
(peach-gold at 30fps, off-palette, reported twice and never fixed) and
`--uitest-wipe-food`, which never worked — **not** "QA cloud pollution"
as E4/E6 both recorded, but `--uitest-seed-program` re-seeding after
the wipe in the same launch. **Below the bar**: the Method corpus
(measured: **62 of 84 lessons carry female-coded language** — an era
of its own) · Method/breathwork photography · the workout library ·
**Home's hero carousel still opens on calories** (the same law, still
inverted on the most-seen surface — fix this first) · the desk's dead
space. **Next era per §12: THE MERGE** — seven eras are stacked on an
unmerged branch and none of them, including E7, can be falsified until
they meet a payer.

**APP v25 E6 — THE DESK (feat/app-v2). BUILT 2026-08-11.**
`docs/app_v25/19_E6_DECISION_AND_RECORD.md` is the why + the record.
Founder steer of the same date: **E5 THE FIRST PLATE SHIPS OFF** —
the hard-paywall funnel is under an active production test and
proof-before-paywall is a separate experiment. Production order is
unchanged (onboarding → hard paywall → purchase/entitlement → jeni);
the flag is now an explicit ENABLE (`e5.firstPlate.enabled`, default
false) because a `disabled` key fails OPEN. Both states verified.
**THE DATA STOPPED**: with the paywall staying, the addressable
population is payers (~2/day, median 2.0 active days), and an attempt
to let day-0 actions pick the era FAILED — breathwork is a scripted
step of `PostPurchaseFlowView`, and 105 of 160 `main_tab_appeared`
users never purchased on a hard-gated app (internal builds). Production
data cannot currently discriminate between in-app features; the binding
constraint is quality. **Three things I expected and found wrong** (all
caught before building): the food reading is NOT a spreadsheet
(`--uitest-plate-detail` opens the Home sheet, not `SnapResultView`);
the three food entrances ALREADY converge on one reading; the describe
header's ♥ is a SANCTIONED brand mark per `JKMarks`. **THE DESK**: the
chat's resting line goes from claim to proof in the same real estate
("your coach, day to day." → "4 plates and 123 g of protein, on
file.") via `JeniDeskAwareness`, a pure engine over the SAME
`TodayStateService.snapshot` the starters read; empty falls back to the
claim, "0 g" never renders, a gap is stated warmly and pinned against
ten reprimand words, the E4 G9 care gate survives. **869/869 app +
125/125 package.** New door: `--uitest-wipe-chat`. **Below the bar,
observed NOT fixed**: the steps detail's orange gradient ring
(off-palette) · the Method reader's female-only photography + its
"she's being good today" lesson line and breathwork's imagery (unisex
debt in an 84-lesson corpus + asset library) · the reading leads with
kcal where the law says protein leads.

**APP v25 E5 — THE FIRST PLATE (feat/app-v2). BUILT 2026-08-11; SHIPS
OFF behind `e5.firstPlate.enabled`.**
`docs/app_v25/17_E5_DECISION.md` is why this era REPLACED the roadmap's
dispersal era; `18_E5_EVIDENCE.md` the record. **The finding: every era
before this asked why people don't come back; this one asked who ever
arrives. Measured per app version — 1.1.6: 236 onboarded → 18 saw the
main app (7.6%) → 20 purchased; 1.1.5: 200 → 21 (10.5%) → 18; 1.1.4:
103 → 6 (5.8%) → 6. Essentially everyone who gets in is a purchaser.
`paywall_view` 2,436 users → 172 purchases (7.1%): the wall is the
most-seen surface Jeni has. Payers (n=151) have a median 2.0 active
days. E1-E4 currently serve ~20 people per release.** The gate is
`AppPhase.swift:84`; `00_THE_SYSTEM` §12 already BANNED "paywalling the
record" and r1 §9 already named a competitor's paywalled log as the
trust opening — the product was doing the forbidden thing. The era:
proof before the ask. THE PROOF PHASE (`AppPhase.proof` mounts the REAL
`CaptureFlowView` + real vision EF + real `FoodLogPersister`; ONE plate;
it persists so paying later opens a record that already has it; pure
`derive`, exclusion set pinned: resolved · ever-entitled · legacy
footprint · entitled · flag off) → WHAT IT MEANS (`FirstPlateReading
Engine`, pure, 12-case honesty table: protein leads, kcal only when
protein cannot, NO floor without a weight on file, coarse words never
percentages, no verdict; floor = `TargetsService.proteinTargetG`) → THE
WALL EARNED (`WallReason.afterProof` on ExpiredWelcomeView's two-state
precedent, so **PaywallView is untouched** — price/tiers/bands/downsell
/exit-intent all unchanged) → THE SCAN CHOOSER REBUILT (founder's direct
ask: nested tiles gone, four geometries → two, bottom-anchored,
ink-over-blur, meal door FIRST, **the doors are made of her record** —
her last photographed plate on the meal door, the dish named on the
again door; the body door stays drawn per L4). Found by looking, not
testing: the food-AI consent sheet named **Anthropic** as a recipient of
her photo when nothing in the repo calls it (corrected); a QA door
overrode the outcome getter so a walker leg passed while sitting on the
invite (rewired + assertion strengthened); an empty capture now gets ONE
retry. **857/857 app + 125/125 package; walker 4/4.** Doors:
--uitest-force-first-plate · --uitest-first-plate-done ·
--uitest-first-plate-noweight · --uitest-wipe-food. Founder gates: the
standing set + **the business call** (this changes the ORDER of the ask,
not the model; kill switch `e5.firstPlate.disabled` restores the exact
pre-E5 gate in one line) + device walk + post-merge read of
`first_plate_*`. Named debt: the ~128 `woman-doing-X` workout animations
are 100% female-presenting and ARE reached (203 users/90d) — reported,
not fixed, because the fix is roadmap E3's library kill.

**APP v25 E4 — DAY TWO (feat/app-v2). BUILT 2026-08-11; rides the
same RC 1.2.0 (30). NO migration.**
`docs/app_v25/14_E4_DECISION.md` is why (the founder's day-one
brief + fresh PostHog: **day-0 food loggers return at 76% vs 17%;
the scan funnel completes 100% once started but 3.4% ever start; a
day-1 user who logged 2 meals + a weight woke to ONE berry ring**);
`15_E4_DAY_TWO.md` the law; `16_E4_EVIDENCE.md` the record. The
era: everything given on day N returns as understanding on day N+1.
THE MORNING READ (`DailyBriefEngine.YesterdayReceipt` + the week-one
day-two clause; kept-promise finally reachable, "proud" read back,
first weigh-in acknowledged; prose/ledger de-dup law) → THE PLATE'S
MEMORY (corrections PERSIST via payload jsonb; `PlatePriors` pure
engine — corrected dishes only, exact-title, ±15% band, revertible,
PHOTO only; "your numbers" row + "use the scan"; AGAIN ships — the
chooser's third door → RecentMealsSheet, ≤3 taps; cuisine-profile
threading bug dead) → ONE CHOKEPOINT (any plate marks the beat —
camera/book/again/jeni) → ROUTES ARRIVE (becoming consumes its own;
jenifit://plates opens THE BOOK; recap shows the whole day) → THE
BRAIN UNSTARVED (ladder 7→3 — five ids had saturated the budget
forever; the morning rung carries yesterday's record; milestone
retry; lapse_support's dead branch fixed). Becoming's 13-row
zero-wall compresses to one sentence + disclosure; desk subtitle
care-gated. **830/830 app + 125/125 package; the DAY-TWO LOOP
machine-walked green (again → mark-without-camera → the book).**
Doors: --uitest-open-again-sheet · --uitest-seed-week (launch) ·
--uitest-open-food-journal (any tab). Founder gates: the standing
E3 set (deploys, migrations, archive, THE MERGE) + E4.1 (the ONE
clarifying question rides the food-vision deploy) + device walk.
Named debt: QA cloud pollution (deterministic QA account
accumulates seeds, survives erase; needs a wipe door). The gap
map's "THE BOOK has no door" was WRONG — door exists, QA arg was
tab-gated (corrected).

**APP v25 E3 — ONE JENI (feat/app-v2). BUILT 2026-08-11; rides the
same RC 1.2.0 (30). NO migration.**
`docs/app_v25/11_E3_DECISION.md` is why this era REPLACED the
roadmap's movement era; `12_E3_ONE_JENI.md` the law;
`13_E3_EVIDENCE.md` the loop's record + founder gates. **The decision
turned on one number: 82% of everyone who finishes onboarding has
exactly ONE active day (28 of 2,237 ever reach a second week), so
every mechanic five eras shipped speaks only to a tail that barely
exists.** Bloom (arXiv 2510.05449, RCT N=54) supplied the mechanism:
5.6× app time for an LLM with tool access to the user's own data,
write access to the structured plan, and memory — and its lesson that
users named *plans, not chat*. So: the coach can READ her record
(`JeniReadTools`, 8 lookups over the SAME engines the surfaces
render from; honest emptiness — an unlogged day is "not logged",
never zero; suppression + never-brand hold) → REMEMBER what she is
told (`JeniMemoryRecord` + `MemoryGuard` refusing doses/diagnoses/
symptoms/body judgements at the door; written only through a card;
`what jeni remembers` in settings with per-row forget) → CHANGE THE
PLAN IN WORDS (`propose_program_fact` through `ProgramFactStore`:
chat writes `preferred` only, a **prescribed head REFUSES** and
routes, the clamped value is what gets acknowledged) — all through
the same chokepoints as the weekly read. THE TOOL LOOP was the
structural fix: `ChatSession` used to run a tool and stop, so a
read's result could never reach the model. Tools now live
CLIENT-side (`JeniToolCatalog`, allowlisted server-side) — the last
jeni-chat deploy a tool addition needs. **The 08-10 unisex sweep
missed both EF prompts** ("a program for women", "serving gen-z
women"); rewritten, plus the CA/IL/TX identity line ("jeni is a
digital coach. not a person, not your clinician."). 809/809 (+26,
zero regressions); the compounding loop FILMED (a sentence in chat →
Today composes "6,000 steps"); 6 frame-caught fixes. Doors:
--uitest-chat-read · --uitest-chat-propose · --uitest-chat-auto-
confirm · --debug-jeni-memory · --uitest-seed-memory. Founder gates:
**deploy jeni-chat AND food-vision** (the prompt still says "gen-z
women" in production until then) + the standing migrations/merge.
Movement (old E3) and the workout-library kill are DEFERRED, not
cancelled; the method library is NOT killed (132 post-onboarding
openers make it the #2 activity — the audit's REMOVE line was about
the literature, not this corpus).

**APP v25 E2 — THE MEDICATED YEAR (feat/app-v2). BUILT 2026-08-10;
RC 1.2.0 (30) — lands WITH the main-merge release, not behind it.**
`docs/app_v25/08_E2_BRIEF.md` the mandate; `09_E2_MEDICATED_YEAR.md`
the architecture + 10 recon corrections; `10_E2_EVIDENCE.md` the
loop's record + founder gates. The medication platform became part
of the adaptive intelligence: COHORT IDENTITY as PostHog person
properties + the dark v24/E1 events wired + AnalyticsHygiene
allowlist-as-mechanism (the kill/redirect trigger: medicated share
readable post-release) → THE CYCLE (`CyclePosition`: event-anchored
day 1..7, open slot outranks the rhythm, zero daily/non-med leakage)
→ LABEL FACTS on `MedicationCatalog` (7 products verbatim vs
2025/26 FDA PIs, per-label frames, compounded = no-label truth,
routing always closes) → the late door WIRED (openLateSlot → Today
support row + DoseSheet late face + label card; tap/quick-mark/
evening-yes converge on THE SLOT) → food noise + underreported
symptoms (hair/menstrual/cold/mood; mood = 988 support FIRST;
severity re-record fixed) → `foodNoiseReturn` signature observation
(≥3 cycles, 2-day cluster) → WEIGHT INTELLIGENCE
(`WeightWeekReadEngine`: time-aware EMA τ9.5d, clamp, unit-error
rejection, ±0.25%BM/wk band, sufficiency ladder) → THE READ GROWS UP
(dose-week story + weight signal in the band + cycle/era/plateau
teachings under offer-first precedence; richer never longer) → Today
reasons with the cycle ("your dose day. the week starts here";
late-cycle appetite named; evening ask scoped to open-dose evenings)
→ chat one-jeni (cycle_day/basis + open_dose_slot + week{} envelope;
EF cycle rule — founder deploys) → VisitPacket reads the real
symptom timeline → `SnapRefineMerge` correction-scope guard (the
SnappyMeal ablation defense) + FoodCorrectionSheet swept. 783/783
app + 113/113 package (+74/+7, zero regressions); films
frame-inspected (5 frame-caught fixes). NO new migration. Doors:
--uitest-seed-medication late · --uitest-open-side-effects ·
--uitest-expand-mood. Founder gates (10_E2 §5): v24+E1 migrations ·
jeni-chat deploy · key rotation · archive/TestFlight 1.2.0(30) ·
merge feat/app-v2→main · device walk · post-release PostHog read of
the medicated share (kill/redirect).

**APP v25 — THE SYSTEM (feat/app-v2). E1 THE SPINE SHIPPED 2026-08-10.**
`docs/app_v25/00_THE_SYSTEM.md` is the MASTER PRODUCT PLAN (the law
for eras E1-E7); `05_E1_SPINE.md` the build architecture;
`06_E1_EVIDENCE.md` the loop's record. E1 shipped ONE PROGRAM WITH
MEMORY: program_facts authority chains (prescribed › preferred ›
recommended › defaulted; consent-gated recommendations; iOS never
writes prescribed; prescription end RESUMES preference) through
`ProgramFactStore` (the RegimenService law generalized; v4 knobs
write-through) → THE WEEKLY READ (ReSigningView/WeeklyReview
EVOLVED: anchor ladder preference › dose-day › enrollment, composer
signals vs her own usual, v4 rules lead the ONE offer, step-goal
recalc + logging lighten join, 14-day cooldowns) → THE WALKING
ACTION (AdaptiveStepsEngine 60th-percentile-of-own-days; composes
only with a consented goal; "2,100 steps left"; HK workouts absorb;
resolved-goal auto-complete) → THE NOTIFICATION BRAIN (veto arbiter:
≤5/wk hard budget, same-id replaces free, medication exempt from
everything, auto-silence + engagement reset, FNV holdouts) →
lifecycle telemetry (categorical only). 709/709 (+122, zero
regressions); the loop FILMED: read → "let's try it" → fact →
relaunch → Today "0 of 3" with the walk row → survives again.
Doors: --uitest-force-read-day · --uitest-walk-read(-decline) ·
--uitest-read-prefer-steps · --uitest-force-hour N ·
--uitest-steps-today N. Founder gates: apply 20260810090000
migration (stacks after v24's) · device walk · teaching-lines voice
pass. Next eras per 00_THE_SYSTEM §15: E2 THE MEDICATED YEAR → E3
KEEP WHAT YOU BUILT → E4 THE PLATE'S MEMORY → E5 THE DISPERSAL → E6
THE QUEUE → E7 THE GLANCE.

**APP v24 — THE REGIMEN (feat/app-v2). SHIPPED 2026-08-09.**
`docs/app_v24/00_REGIMEN.md` is the law; `01_EVIDENCE.md` the
loop's record. The medication experience rebuilt as a PLATFORM
(MeAgain + Shotsy studied, nothing copied): MedicationCatalog
(9 products; new med = one entry) → RegimenPlanRecord as
append-only VERSION CHAINS (`applySelfRegimen` chokepoint;
supersede never mutate) → DoseEventRecord (deterministic per-slot
ids; every surface converges) → symptoms on the chart. Engines:
schedule (wall clock, DST-safe, weekly late window), rotation
(suggests, never insists), patterns (timing-never-causality;
"picked up after the dose changed"), reminders (FIRST actionable
category: taken / in an hour / log later; never named; survives
breaks). THE DOSE SHEET (site cells pre-selected by rotation, ink
mark, skip reasons, late + oral faces); daily cadence rides as
support OUTSIDE the cap (never dominates); 4 consult beats for
current cohort (clinic door skips all); THE REGIMEN home (facts as
doors + THE RECORD eras + side-effect logger); becoming tile
(tally strip + DOSE ERAS ledger); chat envelope medication{}.
587/587 units; consult walker green. Doors:
--uitest-seed-medication <injectable|oral|b2b|history> ·
--uitest-open-dose-sheet · --uitest-walk-medication. Founder
gates: apply 20260809090000 migration · deploy jeni-chat EF ·
device walk. Tradeoffs (law §11): no PK curve, no site photo, no
lock-screen skip, era ledger over annotated curve.

**APP v23 — THE STILL LIFE (feat/app-v2). SHIPPED 2026-08-07.**
`docs/app_v23/00_STILL_LIFE.md` is the law; `01_EVIDENCE.md` the
loop's record. The food experience reborn from zero: one material
story (glass → understanding → paper → book). THE DIAL (SnapDial —
morphing hairline plate, the reading closes the circle) replaced
brackets+sweep; full-bleed IMMERSION shipped; barcode (live VN +
OpenFoodFacts by code) + label (EF text-hint, zero deploy) modes;
THE READING is ONE page (carousel dead, Result/ subtree deleted,
"add it", no scores); THE BOOK (day spreads, photos lead, month
seams, week read, relog re-homed). PlankFoodTests run via the
package scheme (106/106 — palette pins finally execute); app
557/557. Doors: --food-debug-mode · --uitest-seed-week ·
--uitest-walk-book. Queued: chip→row flash, plate page = reading
read mode, filing beat, device walk, XXXL floors.

**APP v22 — ONE HAND, first pass (feat/app-v2). SHIPPED 2026-08-07.**
`docs/app_v22/00_ONE_HAND.md` is the law: consistency gate, THE
MODULE CONTRACT (B2C/B2B by composition, never forks), propagation
map, THE METHOD rethink (ONE IDEA ONE ACT cards — design bound,
build queued). Shipped: THE FOOD EXPERIENCE — FoodTheme palette came
home (it had drifted a full era; PlankFoodTests isn't in the scheme,
so its pins never ran), plain scan captions + halved sweep + "add it
before you eat", SnapUnderstandingChips (real items land on the
photo — honest theater), protein floor bar + plate split on the
result, sage/amber retired, last heart died. testGrantCameraOnce
primer (sim ignores simctl camera grants). Queued: body motion pass,
moments/chat/settings sweeps, METHOD slice, journal sweep, B2B
registry surfacing.

**APP v21 — THE INSTRUMENT (feat/app-v2). SHIPPED 2026-08-07.**
`docs/app_v21/00_INSTRUMENT.md` + `01_EVIDENCE.md`. The founder's
product redesign after the v13-v20 refinement line (those eras live
in the design law's migration log): the app surfaces communicate
VISUALLY first — the rose ramp became the data language (blush ·
dusty · berry; quantities fill rose, trajectories draw ink,
selection stays ink; clinical stays unadorned); Home = one-line
header + the five-face HERO CAROUSEL (ring with counted numeral
inside) + JeniTaskRow checklist objects (real plate photo chips) +
JeniToolTile destinations with live instruments + the close as a
row; Becoming = numeral-first body card, scope-bar-as-header, rose
tile faces, five-breath detail reveal. 557/557 units; anatomy +
zero-data + gallery legs solo green; XXXL floors walked. New door:
--uitest-walk-carousel. Gotchas: a stale TEST BUNDLE lies (rm the
runner + build-for-testing + watch Compiling); iOS launch snapshots
impersonate the old build after reinstall — wait past the cold
start before judging a capture.

**APP v12 — THE CRAFT PASS (feat/app-v2). SHIPPED 2026-08-07.**
`docs/app_v12/00_CRAFT.md` + `01_EVIDENCE.md`. Architecture
untouched; presentation 100×: the glance layer
(JeniGlance: ring · metric bar · week dots · scope bar · insight
pager + the visibility gate), the chart-craft maturation in
JeniChart (smooth monotone lines, grounded bars, emphasized today;
JeniRibbon/JeniPillBars deleted), Home's nutrition centerpiece
(landed plates MORPH the numeral+ring forward), the living greeting,
tools-as-destinations, directional recap, Becoming's time scopes
(morph never reload) + real mini-chart faces + the weekly insight
carousel (R6 grammar) + deepened detail pages (ledger · WHAT THE
PLAN DOES · provenance), care-first Becoming for clinic patients,
and the evening close's 96pt hero numeral. Film doors:
--debug-gallery-tour · --uitest-walk-strip · --uitest-walk-scope ·
--uitest-open-tile · --uitest-mark-lead · --uitest-land-plate ·
--uitest-care-mode. Synthesized XCUI drags can't scroll the iOS 26.2
sim (probe-proven) — tours film what walkers cannot.

**APP v11 — THE REBIRTH + v11.5 MODERNITY (feat/app-v2). SHIPPED 2026-08-05.**
**`docs/app_v11/00_REBIRTH.md` is THE LAW** (L1-L13); `01_PLAN.md` is
the plan. The founder's brief: the current app disappears; the
architecture and business logic stay; the experience is reborn in the
onboarding's design language. Executed as a **DESIGN PASS** — THE LOOP
(drive the sim → record → dump frames → compare neighbours → fix →
repeat) after every surface; per-screen gate "would Apple ship this?".
**v11.5 THE MODERNITY PASS** (`docs/app_v11/03_MODERNITY.md`, amends
the law): printed page → living surface. JeniSurface (depth without
chrome), JeniCheck (drawn check), JeniPressable, springs everywhere;
the calendar strip is a first-class selector (week paging, disc morph,
the page re-keys to the selected day); TODAY/TOOLS are soft cards;
Becoming's tiles MORPH in-tree into their pages (11 tiles incl.
calories, waist, body fat).
Shape: the editorial kit (7 primitives + motion layer) → JeniChart (one
Canvas engine, SwiftUI Charts dead) → Home from zero (MFP information
architecture: calendar strip → nutrition → TODAY → TOOLS) → Becoming
chart-driven (Apple Fitness Summary IA in paper+ink; body progress
lives HERE, not on Home; 8 provenance-backed tiles incl. fiber, sugar
intake, sodium). Next cycles: S (body scan instrument + result page),
N (Lovi-style scan chooser).

### Standing law (survives every era)

- `docs/app_v9/00_MISSION.md` — L1-L7 product laws (three-questions,
  honesty, body-privacy, passive, register).
- `docs/app_v9/04_DESIGN.md` — the design constitution (ADA bar,
  remove>add), sharpened by v11 §1+§11.
- `docs/jeni_release/00_JENI_RELEASE.md` — brand identity: the
  hand-drawn j mark, one-colour law, paper+ink palette, voice pass
  (hearts retired, dose-dot ornaments, "— jeni").
- `docs/onboarding_v7/00_DIRECTION.md` — onboarding law (persona /
  consequence / evidence / register).
- `docs/app_v8/` — the care platform (Jeni Health › Jeni Care › Jeni);
  S1-S5 shipped; internal dev alpha, test data only, NO BAA — never
  say "HIPAA compliant".
- `docs/glp1_strategy_2026_06_16.md` — cohort strategy + compliance
  floors (no drug brand names, no equivalence claims, no numeric
  weight-loss claims).
- Body privacy: never a number from a photo; BF% via provenance ladder
  only (Health reading, else Deurenberg band); scans local-first,
  backup default OFF; fasting vocabulary never renders;
  observed-never-prescribed enforced in code.

### Shipped history (one line per era; full records in git history)

| era | date | what stands |
|---|---|---|
| v10-v10.4 mirror/relaunch/instrument | 2026-08-04 | WaistCrop + BandProfile laws (§9 of v11 law), rear-camera capture, BodyScan/ modules |
| v9 BODY OS P0-P7 | 2026-08-03/04 | BodyStateService, Body Vision capture, passive weight, care summaries; 488 units |
| onboarding v7 clinical pass | 2026-08-03 | OV5Persona, question/evidence law — live |
| onboarding v6 conversion | 2026-08-02 | keep-wall trust bands, dormant real-proof — live |
| THE JENI RELEASE 1.2.0 | 2026-07-30 | brand + palette + voice — standing law above |
| v8 CARE PLATFORM S1-S5 | 2026-07-28/30 | clinic loop live on dev; pilot founder-gated |
| v7 THE CARE PLAN | 2026-07-27 | CarePlanEngine — still the day composer |
| v6 THE SIGNALS | 2026-07-17 | Signals engine + safety rules — engine law |
| v5 and earlier | 2026-07 | engines survive; layouts long superseded |

The Xcode project name + Bundle ID intentionally stay legacy
(`plankAI` / `com.bk.plankAI`) — renaming forces re-onboarding for
every TestFlight tester; a later founder-gated release handles it.

**Authoritative state doc: `/docs/STATE.md`.** Read it first.

### Auth + sync
- Anonymous-first Supabase auth, Apple + email upgrade, sign-in
  recovery, delete-account + forgot-password (anti-enumeration).
- All entity reads filter via `@Query userId` for cross-account
  isolation. Sign-out sweeps user-scoped `@AppStorage` + cancels
  retention notifications.
- Profile, session_logs, day_progress, weight_logs, session_ratings
  sync via typed Codable upserts; UUID case normalized at hydrate
  boundaries.
- Files: `PlankApp/Auth/`, `PlankApp/Sync/`,
  `Packages/PlankSync/Sources/PlankSync/`.

### Payment (RevenueCat)
- `customerInfoStream` observation. `PaymentService` re-configures on
  `auth.currentUser` changes so sign-in/out doesn't strand prior
  user's entitlement.
- THE KEEP WALL (no-trial, pay-upfront): yearly (badged, pre-selected)
  + quarterly + weekly, billed-today everywhere; v6 earned-trust bands
  + dormant real-proof slot (founder fills verbatim ASC reviews —
  never fabricate). Tier-matched downsell sheets on cancellation
  intent.
- Paywall reads RevenueCat's localized `storeProduct.localizedPriceString`
  per Apple Guideline 3.1.2(a). No hard-coded prices.
- `restore()` respects existing paid users (no re-onboarding).
- Files: `PlankApp/Payment/`, `PlankApp/Views/Paywall/`.

### Onboarding
- v5 architecture (typed state machine, 5 acts, GLP-1 branches) + v6
  conversion evolution + v7 clinical grade pass. `onboarding_version:
  v7`. Read `docs/onboarding_v7/00_DIRECTION.md` before touching.
- QA: `OnboardingV5WalkerUITests` (TEST_RUNNER_GLP1_COHORT,
  TEST_RUNNER_GENDER); StoreKit review-sheet dismissal needed on iOS
  26.2 sim; `--uitest-skip-payment`.
- Files: `PlankApp/Views/OnboardingV5/`.

### Program / plan engines
- `CarePlanEngine` composes the day (gentle tone, clinical lead
  promotions, dose day leads); `ProgramDayPrescription` beats;
  `TargetsService` + `CohortStore` single sources of truth;
  ACSM-grade pacing floors in `ProgramGoalCalculator`; never hardcode
  75 — read `plan.totalDays`.
- Medication first-class: dose day, sit-check, RegimenSheet, verb law
  (add / mark / weigh in).
- Files: `PlankApp/Program/`, `PlankApp/Views/Plan/`.

### Body Vision (BodyScan/)
- Guided on-device scans; rear camera; THE WINDOW fixed-aperture
  capture; WaistCrop (pure, tested — EXIF-normalize before crop) +
  BandProfile (per-row width → words; 3% noise floor; fuller weeks
  never scolded); BodyScanStore local-first; D3 opt-in backup.
- QA: `--uitest-open-body-scan` · `--uitest-scan-allow-manual` ·
  `--uitest-force-scan-day` · `--uitest-scan-simulate-pose` ·
  `--uitest-seed-scans`.
- Files: `PlankApp/BodyScan/`.

### Chat
- Two voices (serif letter + rose marginalia), bare-hairline composer;
  streamed heart emoji stripped by normalizer; EF
  `supabase/functions/jeni-chat`.
- Files: `PlankApp/Chat/`.

### Snap Food (food rail)
- Snap / describe / again modes; camera → vision EF (env-selected
  model; USDA calibration) → 3-slide carousel result (plate panel ·
  jeni note · share composer); PlateEditSession coherent macro↔kcal
  math; sodium/sat-fat/sugar/fiber captured end-to-end; per-ingredient
  ledger rides `food_logs.payload` jsonb.
- Files: `Packages/PlankFood/`.

### Breathwork
- Science-honest primer (cortisol mechanism, NOT fat-burn claims).
- Files: `PlankApp/Views/Welcome/Breath*`.

### Steps + health rails
- HealthKit steps (7,500 anchor), sleep, passive weight (background
  delivery + observers), VitalsService, MovementService.
- Files: `PlankApp/Health/`.

### Launch + loader
- `LaunchBackground` == `bgPrimary` — one continuous surface, no grey
  flash; AffirmationLoaderScreen.
- Files: `PlankApp/Views/Welcome/AffirmationLoaderScreen.swift`,
  `PlankApp/PlankAIApp.swift`.

### Notifications
- Trial-window anchors + daily reminder (`daily_reminder`, surgical
  pending-removal); cohort-aware variants per
  `docs/notification_system_spec_2026_06_16.md`; day-2 consent gates
  first-days pushes (v7).
- Files: `PlankApp/Notifications/`.

### GLP-1 cohort strategy
- Convergence-not-pivot; `Glp1Cohort` enum; cohort signal in the noun
  phrase; bodies reference only shipping features. See
  `docs/glp1_strategy_2026_06_16.md` for the compliance floors.

### Design system
- **`docs/design/00_JENI_DESIGN_LANGUAGE.md` IS THE DESIGN LAW**
  (canonical, 2026-08-06). Philosophy, typography, motion,
  transitions, interaction, haptics, layout, spacing, components,
  animation rules, a11y, copywriting (B2C + B2B), never-do list, and
  good/bad examples. Read it before touching ANY surface. It
  supersedes older design docs where they conflict. The v8 onboarding
  (`docs/onboarding_v8/`) is its reference implementation.
- `PlankApp/DesignSystem/Tokens.swift` is the source of truth. Paper
  `#FCFAF7` + ink `#2A1F1E`; 8 locked tokens; `bgPrimary` is the ONLY
  background. JeniHeroSerif on heroes, Fraunces punch, DMSans body.
- v11 kit: `DesignSystem/Kit/JeniKit.swift` + `JeniMotion.swift` +
  `JeniChart*.swift` — the seven primitives + motion layer are the
  ONLY building blocks on v11 surfaces; default SwiftUI transitions
  banned there.
- Voice: lowercase casual; italic punch via `ItalicAccentText` (never
  `*markers*`); zero hearts; no em-dashes between words; never "AI"
  in user copy; "sugar intake" never "sweetness".
- `JKBorderBeam` placement law in its header (earned surfaces only).
- See `docs/THEME.md`, `docs/her75_typeface_spec_2026_06_10.md`,
  `docs/itgirl_illustration_system_2026_06_12.md`.

### Compliance + metadata
- `MARKETING_VERSION = 1.2.0`, healthcare-fitness category; privacy +
  terms at `jenifit.app`; App Store metadata in
  `docs/app_store_metadata.md`; screenshots spec in
  `docs/APP_STORE_SCREENSHOTS.md`; bundle-size plan in
  `docs/odr_migration_plan.md`.

### QA doors (most-used)
- Post-paywall: `--uitest-inapp-qa --uitest-pro-access`.
- Regimen: `--uitest-seed-regimen`, `--uitest-open-gap 0`.
- Care: `--uitest-care-connect-code`.
- Body: see Body Vision section above.
- Sim gotchas: UI legs run SOLO (unit-suite chaining drops presses);
  incremental builds can skip edits (`touch` + compile-count);
  dedicated QA sim `QA-iPhone16` UDID
  `259952D4-444F-4EFE-864A-F3DD5FBA5D22`; MainActor class deinit
  aborts on iOS 26.2 sim (use @MainActor enum services); Canvas
  animation must self-drive from `.task` phases.

### Open items
- See `TODOS.md`.
- v1.2+ Bundle ID + project rename (founder-gated).
- ElevenLabs voice clip generation pass.

## Skill routing

When the user's request matches an available skill, ALWAYS invoke it using the Skill
tool as your FIRST action. Do NOT answer directly, do NOT use other tools first.
The skill has specialized workflows that produce better results than ad-hoc answers.

Key routing rules:
- Product ideas, "is this worth building", brainstorming → invoke office-hours
- Bugs, errors, "why is this broken", 500 errors → invoke investigate
- Ship, deploy, push, create PR → invoke ship
- QA, test the site, find bugs → invoke qa
- Code review, check my diff → invoke review
- Update docs after shipping → invoke document-release
- Weekly retro → invoke retro
- Design system, brand → invoke design-consultation
- Visual audit, design polish → invoke design-review
- Architecture review → invoke plan-eng-review
- Save progress, checkpoint, resume → invoke checkpoint
- Code quality, health check → invoke health
